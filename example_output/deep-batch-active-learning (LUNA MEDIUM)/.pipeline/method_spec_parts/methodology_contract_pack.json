{"methodology_contract_pack":{"schema_version":"1.0","summary":"The demo must preserve BADGE's uncertainty-aware final-layer gradient embeddings and diverse batch seeding.","core_methodology_element_ids":["gradient-embedding-acquisition"],"implementation_obligations":["Compute per-example final-layer gradient embeddings from predicted probabilities and final-layer features.","Select the batch with k-means++ seeding over those embeddings."],"approved_approximations":[],"forbidden_substitutions":["Do not replace gradient embeddings with entropy, margin, raw probabilities, or input-space features.","Do not replace batch k-means++ seeding with independent pointwise ranking."],"required_controls":["Use one fixed model snapshot for all candidate embeddings.","Return unique indices and respect batch_size."],"verification_expectations":["The live selector computes gradient embeddings before k-means++ seeding.","The selector returns valid unique pool indices."]}}
