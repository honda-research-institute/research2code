{"paper_claims":{"method_description":"The method forms a gradient embedding for each unlabeled example by combining the predicted class probabilities with the gradient of the final linear classifier. It then uses k-means++ seeding on these embeddings to choose a diverse batch that favors uncertain examples.","claimed_results":"The paper reports that BADGE improves active-learning performance over uncertainty-only and diversity-only acquisition methods on image and text classification benchmarks. Its advantage is attributed to selecting batches that are both uncertain and diverse.","benchmark_scale":"The paper evaluates iterative batch acquisition on multiple classification datasets, with batch sizes and budgets varying by benchmark."}}
