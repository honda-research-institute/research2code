{"methodology_replication_contract":{"schema_version":"1.0","elements":[{"element_id":"gradient-embedding-acquisition","role":"core_methodology","replication_status":"must_replicate","paper_section":"Section 3, Algorithm 1","paper_evidence":"BADGE constructs an embedding from the gradient of the loss with respect to the final classification layer and uses it for batch acquisition.","technical_concept":"Uncertainty-aware final-layer gradient embeddings","required_behavior":"Compute one gradient embedding per unlabeled example from predicted probabilities and final-layer features, then use those embeddings for batch selection.","demo_scale_implementation":"Compute the embeddings on the smoke-scale unlabeled pool and select the requested batch with k-means++ seeding.","acceptable_approximations":[],"forbidden_substitutions":["Do not replace gradient embeddings with entropy, margin, raw probabilities, or input-space features.","Do not replace batch k-means++ seeding with independent pointwise ranking."],"required_controls":["Use one fixed model snapshot for all candidate embeddings.","Return unique indices from the supplied unlabeled pool and respect batch_size."],"fairness_checks":["Keep preprocessing and classifier outputs fixed while evaluating the acquisition function.","Do not compare against or implement another acquisition method in the package."],"feasibility_rationale":"The algorithm is specified in the paper and can run on a small CPU-scale pool.","verification_expectations":["The live selector computes final-layer gradient embeddings before k-means++ seeding.","The returned indices are unique and bounded by the pool size."],"verification_probe_refs":[],"blockers":[],"paper_element_ids":["algorithm-1","gradient-embedding"]}]}}
