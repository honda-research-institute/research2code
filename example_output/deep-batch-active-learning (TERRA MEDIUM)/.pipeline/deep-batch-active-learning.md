# DEEP BATCH ACTIVE LEARNING BY DIVERSE, UNCERTAIN GRADIENT LOWER BOUNDS

Jordan T. Ash Princeton University Chicheng Zhang University of Arizona Akshay Krishnamurthy Microsoft Research NYC

John Langford Microsoft Research NYC Alekh Agarwal Microsoft Research Redmond

## ABSTRACT

We design a new algorithm for batch active learning with deep neural network models. Our algorithm, Batch Active learning by Diverse Gradient Embeddings (BADGE), samples groups of points that are disparate and high magnitude when represented in a hallucinated gradient space, a strategy designed to incorporate both predictive uncertainty and sample diversity into every selected batch. Crucially, BADGE trades off between uncertainty and diversity without requiring any hand-tuned hyperparameters. While other approaches sometimes succeed for particular batch sizes or architectures, BADGE consistently performs as well or better, making it a useful option for real world active learning problems.

## 1 INTRODUCTION

In recent years, deep neural networks have produced state-of-the-art results on a variety of important supervised learning tasks. However, many of these successes have been limited to domains where large amounts of labeled data are available. A promising approach for minimizing labeling effort is *active learning*, a learning protocol where labels can be requested by the algorithm in a sequential, feedback-driven fashion. Active learning algorithms aim to identify and label only maximally-informative samples, so that a high-performing classifier can be trained with minimal labeling effort. As such, a robust active learning algorithm for deep neural networks may considerably expand the domains in which these models are applicable.

How should we design a practical, general-purpose, label-efficient active learning algorithm for deep neural networks? Theory for active learning suggests a version-space-based approach (Cohn et al., 1994; Balcan et al., 2006), which explicitly or implicitly maintains a set of plausible models, and queries examples for which these models make different predictions. But when using highly expressive models like neural networks, these algorithms degenerate to querying every example. Further, the computational overhead of training deep neural networks precludes approaches that update the model to best fit data after each label query, as is often done (exactly or approximately) for linear methods (Beygelzimer et al., 2010; Cesa-Bianchi et al., 2009). Unfortunately, the theory provides little guidance for these models.

One option is to use the network's uncertainty to inform a query strategy, for example by labeling samples for which the model is least confident. In a batch setting, however, this creates a pathological scenario where data in the batch are nearly identical, a clear inefficiency. Remedying this issue, we could select samples to maximize batch diversity, but this might choose points that provide little new information to the model.

For these reasons, methods that exploit just uncertainty or diversity do not consistently work well across model architectures, batch sizes, or datasets. An algorithm that performs well when using a ResNet, for example, might perform poorly when using a multilayer perceptron. A diversity-based approach might work well when the batch size is very large, but poorly when the batch size is small. Further, what even constitutes a "large" or "small" batch size is largely a function of the statistical properties of the data in question. These weaknesses pose a major problem for real, practical batch active learning situations, where data are unfamiliar and potentially unstructured. There is no way to know which active learning algorithm is best to use.

Moreover, in a real active learning scenario, every change of hyperparameters typically causes the algorithm to label examples not chosen under other hyperparameters, provoking substantial labeling inefficiency. That is, hyperparameter sweeps in active learning can be label expensive. As a result, active learning algorithms need to "just work", given fixed hyperparameters, to a greater extent than is typical for supervised learning.

Based on these observations, we design an approach which creates diverse batches of examples about which the current model is uncertain. We measure uncertainty as the gradient magnitude with respect to parameters in the final (output) layer, which is computed using the most likely label according to the model. To capture diversity, we collect a batch of examples where these gradients span a diverse set of directions. More specifically, we build up the batch of query points based on these hallucinated gradients using the *k*-MEANS++ initialization (Arthur and Vassilvitskii) [2007), which simultaneously captures both the magnitude of a candidate gradient and its distance from previously included points in the batch. We name the resulting approach Batch Active learning by Diverse Gradient Embeddings (BADGE).

We show that BADGE is robust to architecture choice, batch size, and dataset, generally performing as well as or better than the best baseline across our experiments, which vary all of the aforementioned environmental conditions. We begin by introducing our notation and setting, followed by a description of the BADGE algorithm in Section 3 and experiments in Section 4 We defer our discussion of related work to Section 5

## 2 NOTATION AND SETTING

Define  $[K] := \{1,2,\ldots,K\}$ . Denote by  $\mathcal X$  the instance space and by  $\mathcal Y$  the label space. In this work we consider multiclass classification, so  $\mathcal Y = [K]$ . Denote by D the distribution from which examples are drawn, by  $D_{\mathcal X}$  the unlabeled data distribution, and by  $D_{\mathcal Y|\mathcal X}$  the conditional distribution over labels given examples. We consider the pool-based active learning setup, where the learner receives an unlabeled dataset U sampled according to  $D_{\mathcal X}$  and can request labels sampled according to  $D_{\mathcal Y|\mathcal X}$  for any  $x\in U$ . We use  $\mathbb E_D$  to denote expectation under the data distribution D. Given a classifier  $h: \mathcal X \to \mathcal Y$ , which maps examples to labels, and a labeled example (x,y), we denote the 0/1 error of h on (x,y) as  $\ell_{01}(h(x),y)=I(h(x)\neq y)$ . The performance of a classifier h is measured by its expected 0/1 error, i.e.  $\mathbb E_D[\ell_{01}(h(x),y)]=\Pr_{(x,y)\sim D}(h(x)\neq y)$ . The goal of pool-based active learning is to find a classifier with a small expected 0/1 error using as few label queries as possible. Given a set S of labeled examples (x,y), where each  $x\in S$  is picked from U, followed by a label query, we use  $\mathbb E_S$  as the sample averages over S.

In this paper, we consider classifiers h parameterized by underlying neural networks f of fixed architecture, with the weights in the network denoted by  $\theta$ . We abbreviate the classifier with parameters  $\theta$  as  $h_{\theta}$  since the architectures are fixed in any given context, and our classifiers take the form  $h_{\theta}(x) = \operatorname{argmax}_{y \in [K]} f(x; \theta)_y$ , where  $f(x; \theta) \in \mathbb{R}^K$  is a probability vector of scores assigned to candidate labels, given the example x and parameters  $\theta$ . We optimize the parameters by minimizing the cross-entropy loss  $\mathbb{E}_S[\ell_{\mathrm{CE}}(f(x; \theta), y)]$  over the labeled examples, where  $\ell_{\mathrm{CE}}(p, y) = \sum_{i=1}^K I(y=i) \ln 1/p_i = \ln 1/p_y$ .

#### **Algorithm 1** BADGE: Batch Active learning by Diverse Gradient Embeddings

**Require:** Neural network  $f(x;\theta)$ , unlabeled pool of examples U, initial number of examples M, number of iterations T, number of examples in a batch B.

- 1: Labeled dataset  $S \leftarrow M$  examples drawn uniformly at random from U together with queried labels.
- 2: Train an initial model  $\theta_1$  on S by minimizing  $\mathbb{E}_S[\ell_{CE}(f(x;\theta),y)]$ .
- 3: **for**  $t = 1, 2, \dots, T$ : **do**
- 4: For all examples x in  $U \setminus S$ :
  - 1. Compute its hypothetical label  $\hat{y}(x) = h_{\theta_t}(x)$ .
  - 2. Compute gradient embedding  $g_x = \frac{\partial}{\partial \theta_{\text{out}}} \ell_{\text{CE}}(f(x;\theta), \hat{y}(x))|_{\theta=\theta_t}$ , where  $\theta_{\text{out}}$  refers to parameters of the final (output) layer.
- 5: Compute  $S_t$ , a random subset of  $U \setminus S$ , using the k-MEANS++ seeding algorithm on  $\{g_x : x \in U \setminus S\}$  and query for their labels.
- 6:  $S \leftarrow S \cup S_t$ .
- 7: Train a model  $\theta_{t+1}$  on S by minimizing  $\mathbb{E}_S[\ell_{CE}(f(x;\theta),y)]$ .
- 8: end for
- 9: **return** Final model  $\theta_{T+1}$ .

## 3 ALGORITHM

BADGE, described in Algorithm 1 starts by drawing an initial set of M examples uniformly at random from U and asking for their labels. It then proceeds iteratively, performing two main computations at each step t: a gradient embedding computation and a sampling computation. Specifically, at each step t, for every x in the pool U, we compute the label  $\hat{y}(x)$  preferred by the current model, and the gradient  $g_x$  of the loss on  $(x, \hat{y}(x))$  with respect to the parameters of the last layer of the network. Given these gradient embedding vectors  $\{g_x: x \in U\}$ , BADGE selects a set of points by sampling via the k-MEANS++ initialization scheme (Arthur and Vassilvitskii) 2007). The algorithm queries the labels of these examples, retrains the model, and repeats.

We now describe the main computations — the embedding and sampling steps — in more detail.

The gradient embedding. Since deep neural networks are optimized using gradient-based methods, we capture uncertainty about an example through the lens of gradients. In particular, we consider the model uncertain about an example if knowing the label induces a large gradient of the loss with respect to the model parameters and hence a large update to the model. A difficulty with this reasoning is that we need to know the label to compute the gradient. As a proxy, we compute the gradient as if the model's current prediction on the example is the true label. We show in Proposition I that, assuming a common structure satisfied by most natural neural networks, the gradient norm with respect to the last layer using this label provides a lower bound on the gradient norm induced by any other label. In addition, under that assumption, the length of this hypothetical gradient vector captures the uncertainty of the model on the example: if the model is highly certain about the example's label, then the example's gradient embedding will have a small norm, and vice versa for samples where the model is uncertain (see example below). Thus, the gradient embedding conveys information both about the model's uncertainty and potential update direction upon receiving a label at an example.

**The sampling step.** We want the newly-acquired labeled samples to induce large and diverse changes to the model. To this end, we want the selection procedure to favor both sample magnitude and batch diversity. Specifically, we want to avoid the pathology of, for example, selecting a batch of k similar samples where even just a single label could alleviate our uncertainty on all remaining (k-1) samples.

A natural way of making this selection without introducing additional hyperparameters is to sample from a k-Determinantal Point Process (k-DPP; (Kulesza and Taskar) (2011)). That is, to select a batch of k points

Image /page/3/Figure/1 description: Figure with three side-by-side line plots comparing two selection methods (legend at top: gray = k-DPP, blue = k-means++). Left plot: titled "OpenML#6, MLP, Batch size: 100", y-axis labeled "Accuracy" (range shown from 0.40 to 0.95) and x-axis "#Labels queried" (ticks up to ~16,000). A curve rising steeply from about 0.45–0.50 to ~0.92 by ~2,000 labels and then slowly increasing to ~0.93–0.94 by the right edge; the blue and gray curves are almost overlapping. Middle plot: titled "SVHN, ResNet, Batch size: 1000", y-axis "Accuracy" (range ~0.20–0.90) and x-axis "#Labels queried" (ticks to ~45,000). Both methods start near 0.18–0.20 accuracy, climb quickly to ~0.80 by ~10,000 labels and level off near ~0.88–0.90 by ~40–45k labels; the blue and gray lines are nearly identical with narrow shaded variability bands. Right plot: titled "SVHN, ResNet, Batch size: 1000", y-axis "Time" with a scale indicator "x10^4" and x-axis "#Labels queried" (to ~45,000). Time grows roughly linearly with #labels; the gray k-DPP line has a steeper slope reaching about 7.0×10^4 at the maximum, while the blue k-means++ line is lower, reaching about 3.0×10^4. All plots include light shaded regions around lines indicating variability (confidence intervals or standard deviation).

Figure 1: **Left and center**: Learning curves for k-MEANS++ and k-DPP sampling with gradient embeddings for different scenarios. The performance of the two sampling approaches nearly perfectly overlaps. **Right:** A run time comparison (seconds) corresponding to the middle scenario. Each line is the average over five independent experiments. Standard errors are shown by shaded regions.

with probability proportional to the determinant of their Gram matrix. Recently, Dereziński and Warmuth (2018) showed that in experimental design for least square linear regression settings, learning from samples drawn from a k-DPP can have much smaller mean square prediction error than learning from iid samples. In this process, when the batch size is very low, the selection will naturally favor points with a large length, which corresponds to uncertainty in our space. When the batch size is large, the sampler focuses more on diversity because linear independence, which is more difficult to achieve for large k, is required to make the Gram determinant non-zero.

Unfortunately, sampling from a k-DPP is not trivial. Many sampling algorithms (Kang 2013 Anari et al.) 2016) rely on MCMC, where mixing time poses a significant computational hurdle. The state-of-the-art algorithm of Dereziński (2018) has a high-order polynomial running time in the batch size and the embedding dimension. To overcome this computational hurdle, we suggest instead sampling using the k-MEANS++ seeding algorithm (Arthur and Vassilvitskii) 2007), originally made to produce a good initialization for k-means clustering. k-MEANS++ seeding selects centroids by iteratively sampling points in proportion to their squared distances from the nearest centroid that has already been chosen, which, like a k-DPP, tends to select a diverse batch of high-magnitude samples. For completeness, we give a formal description of the k-MEANS++ seeding algorithm in Appendix A

**Example: multiclass classification with softmax activations.** Consider a neural network f where the last nonlinearity is a softmax, i.e.  $\sigma(z)_i = {e^{z_i}}/{\sum_{j=1}^K e^{z_j}}$ . Specifically, f is parametrized by  $\theta = (W, V)$ , where  $\theta_{\text{out}} = W = (W_1, \dots, W_K)^\top \in \mathbb{R}^{K \times d}$  are the weights of the last layer, and V consists of weights of all previous layers. This means that  $f(x;\theta) = \sigma(W \cdot z(x;V))$ , where z is the nonlinear function that maps an input x to the output of the network's penultimate layer. Let us fix an unlabeled sample x and define  $p_i = f(x;\theta)_i$ . With this notation, we have

$$\ell_{\text{CE}}(f(x;\theta), y) = \ln \left( \sum_{j=1}^{K} e^{W_j \cdot z(x;V)} \right) - W_y \cdot z(x;V).$$

Define  $g_x^y = \frac{\partial}{\partial W} \ell_{\text{CE}}(f(x;\theta),y)$  for a label y and  $g_x = g_x^{\hat{y}}$  as the gradient embedding in our algorithm, where  $\hat{y} = \operatorname{argmax}_{i \in [K]} p_i$ . Then the i-th block of  $g_x$  (i.e. the gradients corresponding to label i) is

$$(g_x)_i = \frac{\partial}{\partial W_i} \ell_{CE}(f(x;\theta), \hat{y}) = (p_i - I(\hat{y} = i))z(x; V). \tag{1}$$

Based on this expression, we can make the following observations:

- 1. Each block of  $g_x$  is a scaling of z(x; V), which is the output of the penultimate layer of the network. In this respect,  $g_x$  captures x's representation information similar to that of Sener and Savarese (2018).
- 2. Proposition below shows that the norm of  $g_x$  is a lower bound on the norm of the loss gradient induced by the example with true label y with respect to the weights in the last layer, that is  $||g_x|| \leq ||g_x^y||$ . This suggests that the norm of  $g_x$  conservatively estimates the example's influence on the current model.
- 3. If the current model  $\theta$  is highly confident about x, i.e. vector p is skewed towards a standard basis vector  $e_j$ , then  $\hat{y} = j$ , and vector  $(p_i I(\hat{y} = i))_{i=1}^K$  has a small length. Therefore,  $g_x$  has a small length as well. Such high-confidence examples tend to have gradient embeddings of small magnitude, which are unlikely to be repeatedly selected by k-MEANS++ at iteration t.

**Proposition 1.** For all  $y \in \{1, ..., K\}$ , let  $g_x^y = \frac{\partial}{\partial W} \ell_{\text{CE}}(f(x; \theta), y)$ . Then

$$||g_x^y||^2 = \Big(\sum_{i=1}^K p_i^2 + 1 - 2p_y\Big)||z(x;V)||^2.$$

Consequently,  $\hat{y} = \operatorname{argmin}_{y \in [K]} \|g_x^y\|$ .

*Proof.* Observe that by Equation (1),

$$||g_x^y||^2 = \sum_{i=1}^K (p_i - I(y=i))^2 ||z(x;V)||^2 = (\sum_{i=1}^K p_i^2 + 1 - 2p_y) ||z(x;V)||^2.$$

The second claim follows from the fact that  $\hat{y} = \operatorname{argmax}_{y \in [K]} p_y$ .

This simple sampler tends to produce diverse batches similar to a k-DPP. As shown in Figure 1 switching between the two samplers does not affect the active learner's statistical performance but greatly improves its computational performance. Appendix G compares run time and test accuracy for both k-MEANS++ and k-DPP based sampling based on the gradient embeddings of the unlabeled examples.

Figure 2 illustrates the batch diversity and average gradient magnitude per selected batch for a variety of sampling strategies. As expected, both k-DPPs and k-MEANS++ tend to select samples that are diverse (as measured by the magnitude of their Gram determinant) and high magnitude. Other samplers, such as furthest-first traversal for k-Center clustering (FF-k-CENTER), do not seem to have this property. The FF-k-CENTER algorithm is the sampling choice of the CORESET approach to active learning, which we describe in the proceeding section (Sener and Savarese). Appendix F discusses diversity with respect to uncertainty-based approaches.

Appendix B provides further justification for why BADGE yields better updates than vanilla uncertainty sampling in the special case of binary logistic regression (K = 2 and z(x; V) = x).

## 4 EXPERIMENTS

We evaluate the performance of BADGE against several algorithms from the literature. In our experiments, we seek to answer the following question: How robust are the learning algorithms to choices of neural network architecture, batch size, and dataset?

To ensure a comprehensive comparison among all algorithms, we evaluate them in a batch-mode active learning setup with M=100 being the number of initial random labeled examples and batch size B varying from  $\{100,1000,10000\}$ . The following is a list of the baseline algorithms evaluated; the first performs representative sampling, the next three are uncertainty based, the fifth is a hybrid of representative and uncertainty-based approaches, and the last is traditional supervised learning.

Image /page/5/Figure/1 description: A three-panel figure comparing four selection methods (legend at top: k-DPP in gray, k-means++ in blue, Rand in purple, FF k-center in cyan). Each panel has a grid and x-axis labeled "#Labels queried". Left panel: titled "OpenML #6, MLP, Batch size: 100" with y-axis labeled "Log determinant of batch". The x-axis runs 0 to 14,000 and the y-axis ranges approximately from +500 down to −1,250 with ticks at 500, 250, 0, −250, −500, −750, −1,000, −1,250. Four curves start near positive values and steadily decline as more labels are queried. The blue (k-means++) curve remains the highest (least negative) across the run, the gray (k-DPP) is intermediate, the purple (Rand) is lower, and the cyan (FF k-center) is the lowest and most negative by the end around −1,000 to −1,200. Middle panel: titled "SVHN, ResNet, Batch size: 1000" with the same y-label "Log determinant of batch." The x-axis runs 0 to 50,000 and the y-axis from about 0 down to −35,000 (ticks approximately 0, −5,000, −10,000, −15,000, −20,000, −25,000, −30,000, −35,000). All four methods drop sharply early; k-means++ (blue) remains the least negative overall, k-DPP (gray) is slightly worse, Rand (purple) is lower still, and FF k-center (cyan) goes to the most negative values (around −30,000 to −35,000) before a modest rebound. Right panel: titled "OpenML #6, MLP, Batch size: 100" with y-axis labeled "Average ℓ2 norm in batch." The x-axis runs 0 to 14,000 and the y-axis from 0 to 35 with ticks at 0, 5, 10, 15, 20, 25, 30, 35. All four curves start high (~25–35) and decrease as more labels are queried; k-means++ (blue) and k-DPP (gray) start highest (~30–35) and decline to about 5, while Rand (purple) and FF k-center (cyan) start lower and also converge near ~5. Overall the figure contrasts how the four selection methods affect batch log-determinant and average L2 norm across queries for two datasets/models.

Figure 2: A comparison of batch selection algorithms using our gradient embedding. **Left and center:** Plots showing the log determinant of the Gram matrix of the selected batch of gradient embeddings as learning progresses. **Right:** The average embedding magnitude (a measurement of predictive uncertainty) in the selected batch. The FF-k-CENTER sampler finds points that are not as diverse or high-magnitude as other samplers. Notice also that k-MEANS++ tends to actually select samples that are both more diverse and higher-magnitude than a k-DPP, a potential pathology of the k-DPP's degree of stochastisity. Standard errors are shown by shaded regions.

- 1. CORESET: A diversity-based approach using coreset selection. The embedding of each example is computed by the network's penultimate layer and the samples at each round are selected using a greedy furthest-first traversal conditioned on all labeled examples (Sener and Savarese) [2018).
- 2. Confidence Sampling): An uncertainty-based active learning algorithm that selects B examples with smallest predicted class probability,  $\max_{i=1}^{K} f(x; \theta)_i$  (e.g. Wang and Shang) 2014).
- 3. MARG (Margin Sampling): An uncertainty-based active learning algorithm that selects the bottom B examples sorted according to the example's multiclass margin, defined as  $f(x;\theta)_{\hat{y}} f(x;\theta)_{y'}$ , where  $\hat{y}$  and y' are the indices of the largest and second largest entries of  $f(x;\theta)$  (Roth and Small) [2006).
- 4. Entropy: An uncertainty-based active learning algorithm that selects the top B examples according to the entropy of the example's predictive class probability distribution, defined as  $H((f(x;\theta)_y)_{y=1}^K)$ , where  $H(p) = \sum_{i=1}^K p_i \ln 1/p_i$  (Wang and Shang), 2014).
- 5. ALBL (Active Learning by Learning): A bandit-style meta-active learning algorithm that selects between CORESET and CONF at every round (Hsu and Lin) 2015).
- 6. RAND: The naive baseline of randomly selecting k examples to query at each round.

We consider three neural network architectures: a two-layer Perceptron with ReLU activations (MLP), an 18-layer convolutional ResNet (He et al.) [2016), and an 11-layer VGG network (Simonyan and Zisserman) [2014]. We evaluate our algorithms using three image datasets, SVHN (Netzer et al.) [2011], CIFAR10 (Krizhevsky [2009]) and MNIST (LeCun et al.) [1998] [1] and four non-image datasets from the OpenML repository (#6, #155, #156, and #184). [2] We study each situation with 7 active learning algorithms, including BADGE, making for 231 total experiments.

For the image datasets, the embedding dimensionality in the MLP is 256. For the OpenML datasets, the embedding dimensionality of the MLP is 1024, as more capacity helps the model fit training data. We fit

<sup>&</sup>lt;sup>1</sup>Because MNIST is a dataset that is extremely easy to classify, we only use MLPs, rather than convolutional networks, to better study the differences between active learning algorithms.

<sup>&</sup>lt;sup>2</sup>The OpenML datasets are from openml.org and are selected on two criteria: first, they have at least 10000 samples; second, neural networks have a significantly smaller test error rate when compared to linear models.

Image /page/6/Figure/1 description: A three-panel line-plot figure comparing active learning methods. At the top is a horizontal legend with colored labels: ALBL (light blue), Conf (gold/yellow), Coreset (teal/green), BADGE (dark blue), Entropy (brown), Marg (orange), Rand (purple). Each subplot shows mean accuracy curves with shaded bands (uncertainty) as more labels are queried. Common y-axis label on left is "Accuracy." x-axes are labeled "#Labels queried." Panels (a), (b), (c) are arranged left-to-right: 

Left panel (a) title: "SVHN, ResNet, Batch size: 100." X-axis range shown from 0 to 35000 with ticks at roughly 5000, 10000, 15000, 20000, 25000, 30000, 35000. Y-axis runs from 0.10 to 0.90 (0.10, 0.20, ... 0.90). Multiple colored curves rise from about 0.20 accuracy at small label counts up toward ~0.88–0.90 as labels approach 35000; the methods perform similarly with overlapping shaded bands.

Center panel (b) title: "OpenML#156, MLP, Batch size: 1000." X-axis ranges 0–4000 with ticks at 500, 1000, 1500, 2000, 2500, 3000, 3500, 4000. Y-axis from 0.65 to 0.90. Curves start near ~0.67 and quickly rise by 1000–1500 labels to between ~0.83 and ~0.90. The Coreset (teal/green) curve lags noticeably below the others, while BADGE, Marg, Entropy, Conf and Rand reach higher accuracies; shaded bands show variability.

Right panel (c) title: "CIFAR10, VGG, Batch size: 10000." X-axis from 0 to 40000 with ticks at 5000, 10000, 15000, 20000, 25000, 30000, 35000, 40000. Y-axis from 0.10 to 0.80. All methods start around 0.10–0.20 and rapidly increase to about 0.75–0.80 by ~10000–15000 labels, then converge and remain near ~0.80 for larger label counts; curves are tightly overlapping with small shaded uncertainty.

Overall the figure compares accuracy trajectories of seven query strategies across three datasets/models and shows that most methods quickly improve and converge, with Coreset notably underperforming on the OpenML MLP task.

Figure 3: Active learning test accuracy versus the number of total labeled samples for a range of conditions. Standard errors are shown by shaded regions.

models using cross-entropy loss and the Adam variant of SGD until training accuracy exceeds 99%. We use a learning rate of 0*.*001 for image data and of 0*.*0001 for non-image data. We avoid warm starting and retrain models from scratch every time new samples are queried (Ash and Adams, 2019). All experiments are repeated five times. No learning rate schedules or data augmentation are used. Baselines use implementations from the libact library (Yang et al., 2017). All models are trained in PyTorch (Paszke et al., 2017).

Learning curves. Here we show examples of learning curves that highlight some of the phenomena we observe related to the fragility of active learning algorithms with respect to batch size, architecture, and dataset.

Often, we see that in early rounds of training, it is better to do diversity sampling, and later in training, it is better to do uncertainty sampling. This kind of event is demonstrated in Figure 3a, which shows CORESET outperforming confidence-based methods at first, but then doing worse than these methods later on.

In this figure, BADGE performs as well as diversity sampling when that strategy does best, and as well as uncertainty sampling once those methods start outpacing CORESET. This suggests that BADGE is a good choice regardless of labeling budget.

Separately, we notice that diversity sampling only seems to work well when either the model has good architectural priors (inductive biases) built in, or when the data are easy to learn. Otherwise, penultimate layer representations are not meaningful, and diverse sampling can be deleterious. For this reason, CORESET often performs worse than random on sufficiently complex data when not using a convolutional network (Figure 3b). That is, the diversity induced by unconditional random sampling can often yield a batch that better represents the data. Even when batch size is large and the model has helpful inductive biases, the uncertainty information in BADGE can give it an advantage over pure diversity approaches (Figure 3c). Comprehensive plots of this kind, spanning architecture, dataset, and batch size are in Appendix C.

Image /page/6/Figure/8 description: A square heatmap matrix comparing seven methods with both row and column labels: BADGE, ALBL, Coreset, Conf, Marg, Entropy, Rand (same order on top and left). Each cell contains a numeric value and is color-coded from purple (near 0) to yellow (around 13) with a vertical color bar on the right labeled 0 to ~13.

Cell values by row (rows labeled on the left):
- BADGE row: [BADGE: 0.0, ALBL: 9.18, Coreset: 10.97, Conf: 12.56, Marg: 3.88, Entropy: 13.16, Rand: 10.04]
- ALBL row: [BADGE: 0.34, ALBL: 0.0, Coreset: 5.18, Conf: 3.15, Marg: 0.31, Entropy: 6.81, Rand: 4.95]
- Coreset row: [BADGE: 1.65, ALBL: 2.02, Coreset: 0.0, Conf: 6.55, Marg: 3.08, Entropy: 8.13, Rand: 6.56]
- Conf row: [BADGE: 0.54, ALBL: 2.78, Coreset: 7.61, Conf: 0.0, Marg: 0.33, Entropy: 5.96, Rand: 6.08]
- Marg row: [BADGE: 0.96, ALBL: 7.14, Coreset: 10.87, Conf: 9.34, Marg: 0.0, Entropy: 11.33, Rand: 9.34]
- Entropy row: [BADGE: 0.31, ALBL: 1.79, Coreset: 6.05, Conf: 1.25, Marg: 0.35, Entropy: 0.0, Rand: 5.12]
- Rand row: [BADGE: 0.84, ALBL: 5.63, Coreset: 7.61, Conf: 8.67, Marg: 2.98, Entropy: 10.65, Rand: 0.0]

Below the main matrix there is a white horizontal divider and a smaller summary row (likely mean or aggregate differences) with values: [0.66, 4.08, 6.9, 5.93, 1.56, 8.01, 6.01].

The diagonal cells are 0.0. The largest observed value in the heatmap is 13.16 (BADGE vs Entropy), shown in the brightest yellow; small values near 0 are purple.

Figure 4: A pairwise penalty matrix over all experiments. Element *Pi,j* corresponds roughly to the number of times algorithm *i* outperforms algorithm *j*. Column-wise averages at the bottom show overall performance (lower is better).

**Pairwise comparisons.** We next show a comprehensive pairwise comparison of algorithms over all datasets (D), batch sizes (B), model architectures (A), and label budgets (L). From the learning curves, it can be observed that when label budgets are large enough, all algorithms eventually reach similar performance, making the comparison between them uninteresting in the large sample limit. For this reason, for each combination of (D,B,A), we select a set of labeling budgets L where learning is still progressing. We experimented with three different batch sizes and eleven dataset-architecture pairs, making the total number of (D,B,A) combinations  $3 \times 11 = 33$ . Specifically, we compute  $n_0$ , the smallest number of labels where RAND's accuracy reaches 99% of its final accuracy, and choose label budget L from  $\{M+2^{m-1}B: m \in [\lfloor \log((n_0-M)/B)\rfloor]\}$ . The calculation of scores in the penalty matrix P follows the following protocol: For each (D,B,A,L) combination and each pair of algorithms (i,j), we have 5 test errors (one for each repeated run),  $\{e_i^1,\ldots,e_i^5\}$  and  $\{e_i^1,\ldots,e_i^5\}$  respectively. We compute the t-score as  $t=\sqrt{5}\hat{\mu}/\hat{\sigma}$ , where

$$\hat{\mu} = \frac{1}{5} \sum_{l=1}^{5} (e_i^l - e_j^l), \qquad \hat{\sigma} = \sqrt{\frac{1}{4} \sum_{l=1}^{5} (e_i^l - e_j^l - \hat{\mu})^2}.$$

We use the two-sided t-test to compare pairs of algorithms: algorithm i is said to beat algorithm j in this setting if t>2.776 (the critical point of p-value being 0.05), and similarly algorithm j beats algorithm i if t<-2.776. For each (D,B,A) combination, suppose there are  $n_{D,B,A}$  different values of L. Then, for each L, if algorithm i beats algorithm j, we accumulate a penalty of  $1/n_{D,B,A}$  to  $P_{i,j}$ ; otherwise, if algorithm j beats algorithm i, we accumulate a penalty of  $1/n_{D,B,A}$  to  $P_{j,i}$ . The choice of the penalty value  $1/n_{D,B,A}$  is to ensure that every (D,B,A) combination is assigned equal influence in the aggregated matrix. Therefore, the largest entry of P is at most 33, the total number of (D,B,A) combinations.

Image /page/7/Figure/4 description: A cumulative distribution plot of normalized error for several selection methods. The x-axis is labeled “Normalized error” and runs from 0.3 to 1.2; the y-axis is labeled “Cumulative frequency” and runs from 0.0 to 1.0. A vertical magenta (purple) line is drawn at x = 1.0. Seven method curves are shown with a legend mapping colors to methods: BADGE (blue), ALBL (light cyan), Coreset (green), Conf (yellow), Marg (orange-brown), Entropy (brown), and Rand (magenta for the vertical line). All curves stay near 0 cumulative frequency at low normalized error (0.3–0.7), then begin to rise after about 0.75. Most curves show a steep increase between roughly 0.9 and 1.1. At the vertical line x = 1.0 the methods have differing cumulative frequencies (approximately in the 0.4–0.8 range depending on method). By x ≈ 1.15–1.2 most curves approach cumulative frequency values near 0.9–1.0. The plot includes gridlines and a boxed legend centered below the plot.

Figure 5: The cumulative distribution function of normalized errors for all acquisition functions.

Intuitively, each row i indicates the number of settings in which algorithm i beats other algorithms and each column j indicates the number of settings in which algorithm j is beaten by another algorithm.

The penalty matrix in Figure 4 summarizes all experiments, showing that BADGE generally outperforms baselines. Matrices grouped by batch size and architecture in Appendix D show a similar trend.

Cumulative distribution functions of normalized errors. For each (D,B,A,L) combination, we compute the average error for each algorithm i as  $\bar{e}_i = \frac{1}{5} \sum_{l=1}^5 e_l^l$ . To ensure that the errors of these algorithms are on the same scale in all settings, we compute the normalized error of every algorithm i, defined as  $\text{ne}_i = \bar{e}_i/\bar{e}_r$ , where r is the index of the RAND algorithm. By definition, the normalized errors of the RAND algorithm are identically 1 in all settings. Like with penalty matrices, for each (D,B,A) combination, we only consider a subset of L values from the set  $\{M+2^{m-1}B:m\in[\lfloor\log((n_0-M)/B)\rfloor]\}$ . We assign a weight proportional to  $1/n_{D,B,A}$  to each (D,B,A,L) combination, where there are  $n_{D,B,A}$  different L values for this combination of (D,B,A). We then plot the cumulative distribution functions (CDFs) of the normalized errors of all algorithms: for a value of x, the y value is the total weight of settings where the algorithm has normalized error at most x; in general, an algorithm that has a higher CDF value has better performance.

We plot the generated CDFs in Figures [5] [22] and [23] We can see from Figure [5] that BADGE has the best overall performance. In addition, from Figures [22] and [23] in Appendix [E] we can conclude that when batch size is small (100 or 1000) or when an MLP is used, both BADGE and MARG perform best. However, in the regime when the batch size is large (10000), MARG's performance degrades, while BADGE, ALBL and CORESET are the best performing approaches.

## 5 RELATED WORK

Active learning is a been well-studied problem (Settles, 2010; Dasgupta, 2011; Hanneke, 2014). There are two major strategies for active learning—representative sampling and uncertainty sampling.

Representative sampling algorithms select batches of unlabeled examples that are representative of the unlabeled set to ask for labels. It is based on the intuition that the sets of representative examples chosen, once labeled, can act as a surrogate for the full dataset. Consequently, performing loss minimization on the surrogate suffices to ensure a low error with respect to the full dataset. In the context of deep learning, Sener and Savarese (2018); Geifman and El-Yaniv (2017) select representative examples based on core-set construction, a fundamental problem in computational geometry. Inspired by generative adversarial learning, Gissin and Shalev-Shwartz (2019) select samples that are maximally indistinguishable from the pool of unlabeled examples.

On the other hand, uncertainty sampling is based on a different principle—to select new samples that maximally reduce the uncertainty the algorithm has on the target classifier. In the context of linear classification, Tong and Koller (2001); Schohn and Cohn (2000); Tur et al. (2005) propose uncertainty sampling methods that query examples that lie closest to the current decision boundary. Some uncertainty sampling approaches have theoretical guarantees on statistical consistency (Hanneke, 2014; Balcan et al., 2006). Such methods have also been recently generalized to deep learning. For instance, Gal et al. (2017) use Dropout as an approximation of the posterior of the model parameters, and develop information-based uncertainty reduction criteria; inspired by recent advances on adversarial examples generation, Ducoffe and Precioso (2018) use the distance between an example and one of its adversarial examples as an approximation of its distance to the current decision boundary, and uses it as the criterion of label queries. An ensemble of classifiers could also be used to effectively estimate uncertainty (Beluch et al., 2018).

There are several existing approaches that support a hybrid of representative sampling and uncertainty sampling. For example, Baram et al. (2004); Hsu and Lin (2015) present meta-active learning algorithms that can combine the advantages of different active learning algorithms. Inspired by expected loss minimization, Huang et al. (2010) develop label query criteria that balances between the representativeness and informativeness of examples. Another method for this is Active Learning by Learning (Hsu and Lin, 2015), which can select whether to exercise a diversity based algorithm or an uncertainty based algorithm at each round of training as a sequential decision process.

There is also a large body of literature on batch mode active learning, where the learner is asked to select a batch of samples within each round (Guo and Schuurmans, 2008; Wang and Ye, 2015; Chen and Krause; Wei et al., 2015; Kirsch et al., 2019). In these works, batch selection is often formulated as an optimization problem with objectives based on (upper bounds of) average log-likelihood, average squared loss, etc.

A different query criterion based on expected gradient length (EGL) has been proposed in the as well (Settles et al., 2008). In recent work, Huang et al. (2016) show that the EGL criterion is related to the *T*-optimality criterion in experimental design. They further demonstrate that the samples selected by EGL are very different from those by entropy-based uncertainty criterion. Zhang et al. (2017a) use the EGL criterion in active sentence and document classification with CNNs. These approaches differ most substantially from BADGE in that they do not take into account the diversity of the examples queried within each batch.

There is a wide array of theoretical articles that focus on the related problem of adaptive subsampling for fully-labeled datasets in regression settings (Han et al., 2016; Wang et al., 2018; Ting and Brochu, 2018). Empirical studies of batch stochastic gradient descent also employ adaptive sampling to "emphasize" hard or representative examples (Zhang et al., 2017b; Chang et al., 2017). These works aim at reducing computation costs or finding a better local optimal solution, as opposed to reducing label costs. Nevertheless, our work is inspired by their sampling criteria, which also emphasize samples that induce large updates to the model.

As mentioned earlier, our sampling criterion has resemblance to sampling from *k*-determinantal point processes (Kulesza and Taskar, 2011). Note that in multiclass classification settings, our gradient-based embedding of an example can be viewed as the outer product of the original embedding in the penultimate layer and a probability score vector that encodes the uncertainty information on this example (see Section 3). In this view, the penultimate layer embedding characterizes the diversity of each example, whereas the probability score vector characterizes the quality of each example. The *k*-DPP is also a natural probabilistic tool for sampling that trades off between quality and diversity (See Kulesza et al., 2012, Section 3.1). We remark that concurrent to our work, Bıyık et al. (2019) develops *k*-DPP based active learning algorithms based on this principle by explicitly designing diversity and uncertainty measures.

## 6 DISCUSSION

We have established that BADGE is empirically an effective deep active learning algorithm across different architectures and batch sizes, performing similar to or better than other active learning algorithms. A fundamental remaining question is: "Why?" While deep learning is notoriously difficult to analyze theoretically, there are several intuitively appealing properties of BADGE:

- 1. The definition of uncertainty (a lower bound on the gradient magnitude of the last layer) guarantees some update of parameters.
- 2. It optimizes for diversity as well as uncertainty, eliminating a failure mode of choosing many identical uncertain examples in a batch, and does so without requiring any hyperparameters.
- 3. The randomization associated with the *k*-MEANS++ initialization sampler implies that, even for adversarially constructed datasets, it eventually converges to a good solution.

The combination of these properties appears to generate the robustness that we observe empirically.

## REFERENCES

David Cohn, Les Atlas, and Richard Ladner. Improving generalization with active learning. *Machine learning*, 1994.

Maria-Florina Balcan, Alina Beygelzimer, and John Langford. Agnostic active learning. In *International Conference on Machine Learning*, 2006.

Alina Beygelzimer, Daniel J Hsu, John Langford, and Tong Zhang. Agnostic active learning without constraints. In *Neural Information Processing Systems*, 2010.

Nicolo Cesa-Bianchi, Claudio Gentile, and Francesco Orabona. Robust bounds for classification via selective sampling. In *International Conference on Machine Learning*, 2009.

David Arthur and Sergei Vassilvitskii. k-means++: The advantages of careful seeding. In *ACM-SIAM symposium on Discrete algorithms*, 2007.

Alex Kulesza and Ben Taskar. k-dpps: Fixed-size determinantal point processes. In *International Conference on Machine Learning*, 2011.

Michał Derezinski and Manfred K Warmuth. Reverse iterative volume sampling for linear regression. ´ *The Journal of Machine Learning Research*, 19(1), 2018.

Byungkon Kang. Fast determinantal point process sampling with application to clustering. In *Neural Information Processing Systems*, 2013.

- Nima Anari, Shayan Oveis Gharan, and Alireza Rezaei. Monte carlo markov chain algorithms for sampling strongly rayleigh distributions and determinantal point processes. In *Conference on Learning Theory*, 2016.
- Michał Derezinski. Fast determinantal point processes via distortion-free intermediate sampling. ´ *arXiv preprint*, 2018.
- Ozan Sener and Silvio Savarese. Active learning for convolutional neural networks: A core-set approach. In *International Conference on Learning Representations*, 2018.
- Dan Wang and Yi Shang. A new active labeling method for deep learning. In *2014 International joint conference on neural networks*, 2014.
- Dan Roth and Kevin Small. Margin-based active learning for structured output spaces. In *European Conference on Machine Learning*, 2006.
- Wei-Ning Hsu and Hsuan-Tien Lin. Active learning by learning. In *Association for the advancement of artificial intelligence*, 2015.
- Kaiming He, Xiangyu Zhang, Shaoqing Ren, and Jian Sun. Deep residual learning for image recognition. In *Proceedings of the IEEE conference on computer vision and pattern recognition*, pages 770–778, 2016.
- Karen Simonyan and Andrew Zisserman. Very deep convolutional networks for large-scale image recognition. *arXiv preprint*, 2014.
- Yuval Netzer, Tao Wang, Adam Coates, Alessandro Bissacco, Bo Wu, and Andrew Y Ng. Reading digits in natural images with unsupervised feature learning. 2011.
- Alex Krizhevsky. Learning multiple layers of features from tiny images. Technical report, Citeseer, 2009.
- Yann LeCun, Léon Bottou, Yoshua Bengio, Patrick Haffner, et al. Gradient-based learning applied to document recognition. *IEEE*, 1998.
- Jordan T Ash and Ryan P Adams. On the difficulty of warm-starting neural network training. *arXiv preprint*, 2019.
- Yao-Yuan Yang, Shao-Chuan Lee, Yu-An Chung, Tung-En Wu, Si-An Chen, and Hsuan-Tien Lin. libact: Pool-based active learning in python. *arXiv preprint*, 2017.
- Adam Paszke, Sam Gross, Soumith Chintala, Gregory Chanan, Edward Yang, Zachary DeVito, Zeming Lin, Alban Desmaison, Luca Antiga, and Adam Lerer. Automatic differentiation in pytorch. 2017.
- Burr Settles. Active learning literature survey. *University of Wisconsin, Madison*, 2010.
- Sanjoy Dasgupta. Two faces of active learning. *Theoretical computer science*, 2011.
- Steve Hanneke. Theory of disagreement-based active learning. *Foundations and Trends in Machine Learning*, 2014.
- Yonatan Geifman and Ran El-Yaniv. Deep active learning over the long tail. *arXiv preprint*, 2017.
- Daniel Gissin and Shai Shalev-Shwartz. Discriminative active learning. *arXiv preprint*, 2019.
- Simon Tong and Daphne Koller. Support vector machine active learning with applications to text classification. *Journal of machine learning research*, 2001.
- Greg Schohn and David Cohn. Less is more: Active learning with support vector machines. In *International Conference on Machine Learning*, 2000.

- Gokhan Tur, Dilek Hakkani-Tür, and Robert E Schapire. Combining active and semi-supervised learning for spoken language understanding. *Speech Communication*, 2005.
- Yarin Gal, Riashat Islam, and Zoubin Ghahramani. Deep bayesian active learning with image data. In *International Conference on Machine Learning*, 2017.
- Melanie Ducoffe and Frederic Precioso. Adversarial active learning for deep networks: a margin based approach. *arXiv preprint*, 2018.
- William H Beluch, Tim Genewein, Andreas Nürnberger, and Jan M Köhler. The power of ensembles for active learning in image classification. In *IEEE Conference on Computer Vision and Pattern Recognition*, 2018.
- Yoram Baram, Ran El Yaniv, and Kobi Luz. Online choice of active learning algorithms. *Journal of Machine Learning Research*, 2004.
- Sheng-Jun Huang, Rong Jin, and Zhi-Hua Zhou. Active learning by querying informative and representative examples. In *Neural Information Processing Systems*, 2010.
- Yuhong Guo and Dale Schuurmans. Discriminative batch mode active learning. In *Neural Information Processing Systems*, 2008.
- Zheng Wang and Jieping Ye. Querying discriminative and representative samples for batch mode active learning. *Transactions on Knowledge Discovery from Data*, 2015.
- Yuxin Chen and Andreas Krause. Near-optimal batch mode active learning and adaptive submodular optimization. In *International Conference on Machine Learning*.
- Kai Wei, Rishabh Iyer, and Jeff Bilmes. Submodularity in data subset selection and active learning. In *International Conference on Machine Learning*, 2015.
- Andreas Kirsch, Joost van Amersfoort, and Yarin Gal. Batchbald: Efficient and diverse batch acquisition for deep bayesian active learning. In *Neural Information Processing Systems 32*, 2019.
- Burr Settles, Mark Craven, and Soumya Ray. Multiple-instance active learning. In *Neural Information Processing Systems*, 2008.
- Jiaji Huang, Rewon Child, and Vinay Rao. Active learning for speech recognition: the power of gradients. *arXiv preprint*, 2016.
- Ye Zhang, Matthew Lease, and Byron C Wallace. Active discriminative text representation learning. In *AAAI Conference on Artificial Intelligence*, 2017a.
- Lei Han, Kean Ming Tan, Ting Yang, and Tong Zhang. Local uncertainty sampling for large-scale multi-class logistic regression. *arXiv preprint*, 2016.
- HaiYing Wang, Rong Zhu, and Ping Ma. Optimal subsampling for large sample logistic regression. *Journal of the American Statistical Association*, 2018.
- Daniel Ting and Eric Brochu. Optimal subsampling with influence functions. In *Neural Information Processing Systems*, 2018.
- Cheng Zhang, Hedvig Kjellstrom, and Stephan Mandt. Determinantal point processes for mini-batch diversification. *Uncertainty in Artificial Intelligence*, 2017b.

Haw-Shiuan Chang, Erik Learned-Miller, and Andrew McCallum. Active bias: Training more accurate neural networks by emphasizing high variance samples. In *Neural Information Processing Systems*, 2017.

Alex Kulesza, Ben Taskar, et al. Determinantal point processes for machine learning. *Foundations and Trends in Machine Learning*, 2012.

Erdem Bıyık, Kenneth Wang, Nima Anari, and Dorsa Sadigh. Batch active learning using determinantal point processes. *arXiv* preprint, 2019.

Stephen Mussmann and Percy S Liang. Uncertainty sampling is preconditioned stochastic gradient descent on zero-one loss. In *Neural Information Processing Systems*, 2018.

## A THE k-MEANS++ SEEDING ALGORITHM

Here we briefly review the k-MEANS++ seeding algorithm by (Arthur and Vassilvitskii, 2007). Its basic idea is to perform sequential sampling of k centers, where each new center is sampled from the ground set with probability proportional to the squared distance to its nearest center. It is shown in (Arthur and Vassilvitskii) 2007) that the set of centers returned is guaranteed to approximate the k-means objective function in expectation, thus ensuring diversity.

#### Algorithm 2 The k-MEANS++ seeding algorithm (Arthur and Vassilvitskii 2007)

```
Require: Ground set G \subset \mathbb{R}^d, target size k.

Ensure: Center set C of size k.

C_1 \leftarrow \{c_1\}, where c_1 is sampled uniformly at random from G.

for t = 2, \ldots, k: do

Define D_t(x) := \min_{c \in C_{t-1}} \|x - c\|_2.

c_t \leftarrow \text{Sample } x \text{ from } G \text{ with probability } \frac{D_t(x)^2}{\sum_{x \in G} D_t(x)^2}.

C_t \leftarrow C_{t-1} \cup \{c_t\}.
\nend for return C_k.
```

## B BADGE FOR BINARY LOGISTIC REGRESSION

We consider instantiating BADGE for binary logistic regression, where  $\mathcal{Y} = \{-1, +1\}$ . Given a linear classifier w, we define the predictive probability of w on x as  $p_w(y|x, \theta) = \sigma(yw \cdot x)$ , where  $\sigma(z) = \frac{1}{1+e^{-z}}$  is the sigmoid function.

Recall that  $\hat{y} = \hat{y}(x)$  is the hallucinated label:

$$\hat{y}(x) = \begin{cases} +1, & p_w(+1|x,\theta) > 1/2, \\ -1, & p_w(+1|x,\theta) \le 1/2. \end{cases}$$

The binary logistic loss of classifier w on example (x, y) is defined as:

$$\ell(w, (x, y)) = \ln(1 + \exp(-yw \cdot x)).$$

Now, given model w and example x, we define  $\hat{g}_x = \frac{\partial}{\partial w} \ell(w,(x,\hat{y})) = (1-p_w(\hat{y}|x,\theta)) \cdot (-\hat{y} \cdot x)$  as the loss gradient induced by the example with hallucinated label, and  $\tilde{g}_x = \frac{\partial}{\partial w} \ell(w,(x,y)) = (1-p_w(y|x,\theta)) \cdot (-y \cdot x)$  as the loss gradient induced by the example with true label.

Image /page/13/Figure/1 description: Figure with three side-by-side line plots showing active learning performance on OpenML#6 with an MLP, varying batch size. All plots share y-axis labeled "Accuracy" (range shown ~0.40 to ~0.95) and x-axis labeled "#Labels queried". Each plot title (centered above each subplot) reads: left "OpenML#6, MLP, Batch size: 100", middle "OpenML#6, MLP, Batch size: 1000", right "OpenML#6, MLP, Batch size: 10000". X-axis tick marks are shown at roughly 0, 2000, 4000, 6000, 8000, 10000, 12000, 14000, 16000 on the left and middle plots; the right plot x-axis runs to 10000. Curves for seven methods are plotted with thin colored lines and shaded confidence bands: ALBL (light blue), Conf (yellow), Coreset (green), BADGE (darker blue), Entropy (brown), Marg (orange), and Rand (purple). In the left (batch=100) and middle (batch=1000) plots all methods start around 0.40–0.50 accuracy at very few labels, rise steeply over the first ~2000–6000 labels, and plateau near ~0.90–0.93 by the highest labeled-data points (curves converge near 0.92–0.94). In the right plot (batch=10000) all methods are tightly grouped and increase almost linearly from roughly 0.40 accuracy at 0 labels to about 0.92 accuracy at 10,000 labels, with narrow shaded bands. A legend below the plots lists the seven methods with their colors in the order: ALBL, Conf, Coreset, BADGE, Entropy, Marg, Rand.

Figure 6: Full learning curves for OpenML #6 with MLP.

Image /page/13/Figure/3 description: The image is a three-panel line-plot figure comparing active learning methods on dataset OpenML#155 using an MLP with three different batch sizes. Each panel has the title above it: left: "OpenML#155, MLP, Batch size: 100", middle: "OpenML#155, MLP, Batch size: 1000", right: "OpenML#155, MLP, Batch size: 10000". The x-axis on all panels is labeled "#Labels queried" with tick marks up to 50,000 (major ticks shown at 0, 10,000, 20,000, 30,000, 40,000, 50,000). The y-axis is labeled "Accuracy" and runs from 0.60 up to 1.00. Each panel shows multiple colored accuracy curves that rapidly increase from around 0.60–0.65 at the left toward ~0.95–1.00 as more labels are queried, then plateau near 0.99–1.00 by 50,000 labels. A shared legend centered beneath the three plots lists seven methods and their colors: ALBL (light blue), Conf (yellow), Coreset (teal/green), BADGE (darker blue), Entropy (brown), Marg (orange), and Rand (purple). The curves for the different methods largely overlap in the left and middle panels (batch sizes 100 and 1000), showing a steep rise to ~0.98–1.00 within the first ~5,000–10,000 labels. In the right panel (batch size 10,000) the curves start around 0.60 and show a steeper jump between 0 and ~10,000 labels up to ~0.95, then converge and approach ~0.99–1.00 by 50,000 labels. Gridlines are present in all panels.

Figure 7: Full learning curves for OpenML #155 with MLP.

Suppose that BADGE only selects examples from region  $S_w = \{x : w \cdot x = 0\}$ , then as  $p_w(+1|x,\theta) = p_w(-1|x,\theta) = \frac{1}{2}$ , we have that for all x in  $S_w$ ,  $\hat{g}_x = s_x \cdot g_x$  for some  $s_x \in \{\pm 1\}$ . This implies that, sampling from a DPP induced by  $\hat{g}_x$ 's is equivalent to sampling from a DPP induced by  $g_x$ 's. It is noted in Mussmann and Liang (2018) that uncertainty sampling (i.e. sampling from  $D_{|S_w}$ ) implicitly performs preconditioned stochastic gradient descent on the expected 0-1 loss. In addition, it has been shown that DPP sampling over gradients may reduce the variance of the mini-batch stochastic gradient updates (Zhang et al.) (2017b); this suggests that BADGE, when restricted its sampling over low-margin regions  $(S_w)$ , improves over uncertainty sampling by collecting examples that together induce lower-variance updates on the gradient direction of expected 0-1 loss.

## C ALL LEARNING CURVES

We plot all learning curves (test accuracy as a function of the number of labeled example queried) in Figures 6 to 12. In addition, we zoom into regions of the learning curves that discriminates the performance of all algorithms in Figures 13 to 19

## D PAIRWISE COMPARISONS OF ALGORITHMS

In addition to Figure 4 in the main text, we also provide penalty matrices (Figures 20 and 21), where the results are aggregated by conditioning on a fixed batch size (100, 1000 and 10000) or on a fixed neural network model (MLP, ResNet and VGG). For each penalty matrix, the parenthesized number in its title is the

Image /page/14/Figure/1 description: A three-panel line-plot figure comparing active learning methods on the dataset "OpenML#156, MLP" with three different batch sizes. Each subplot has title, x-axis labeled "#Labels queried" and y-axis labeled "Accuracy" and shows multiple colored accuracy curves with light shading for variance and grid lines. Left panel title: "OpenML#156, MLP, Batch size: 100". X-axis runs from 0 to ~50,000 and y-axis from 0.65 to 0.90. All methods start between ~0.66–0.70 accuracy and gradually climb, most reaching a plateau near 0.90 by the right side of the plot; the green "Coreset" curve is noisier and slower to climb. Middle panel title: "OpenML#156, MLP, Batch size: 1000". X-axis also to ~50,000, y-axis 0.65–0.90. Curves rise faster than in the left panel, with most methods approaching ~0.90 by ~10,000–20,000 labels; some spread between methods is visible in early label counts. Right panel title: "OpenML#156, MLP, Batch size: 10000". X-axis runs to ~40,000 and y-axis 0.65–0.90. Here all methods show a steep initial rise from ~0.68 up to ~0.90, with most curves reaching the ~0.90 level by about 15,000 labels. A legend centered below the three plots identifies the colored methods: ALBL (light blue), Conf (orange/yellow), Coreset (teal/green), BADGE (darker blue), Entropy (brown/beige), Marg (orange), and Rand (purple). Overall the figure shows that larger batch sizes produce faster gains in accuracy (fewer labels needed to reach ~0.90) and that method differences are most pronounced at lower label counts.

Figure 8: Full learning curves for OpenML #156 with MLP.

Image /page/14/Figure/3 description: Three adjacent line plots (learning curves) comparing active learning methods on OpenML#184 using an MLP. Each subplot title indicates the batch size: left "OpenML#184, MLP, Batch size: 100", middle "..., Batch size: 1000", right "..., Batch size: 10000". The shared x-axis label is "#Labels queried" (plots show up to roughly 20,000–25,000 labels); the y-axis label is "Accuracy" with values from about 0.20 to 0.80. Grid lines are shown on all plots. Seven colored method curves are plotted and summarized in a centered legend below the plots: ALBL (light blue), Conf (yellow/gold), Coreset (teal/green), BADGE (dark blue), Entropy (tan/brown), Marg (orange), Rand (purple). All curves start low (approx. 0.25–0.35 accuracy) and rise as more labels are queried. At intermediate label counts (~5,000) accuracies are around 0.50–0.65; by the high end (≈20,000 labels) methods converge near 0.78–0.82. The left (batch size 100) plot shows smoother, more gradual increases up to ~0.80 and modest separation between methods. The middle (batch size 1000) plot shows similar trends with slightly steeper early gains. The right (batch size 10000) plot shows a near-linear rapid rise to ~0.70 by ~10,000 labels and all methods tightly grouped near ~0.80 by 20,000 labels.

Figure 9: Full learning curves for OpenML #184 with MLP.

Image /page/15/Figure/1 description: A 3x3 panel of line plots showing active learning performance on the SVHN dataset. Each subplot has x-axis labeled “#Labels queried” and y-axis labeled “Accuracy.” Rows correspond to model architectures (top: MLP, middle: ResNet, bottom: VGG) and columns correspond to batch sizes (left: 100, center: 1000, right: 10000). A legend along the bottom identifies seven methods by color: ALBL (light blue), Conf (gold/yellow), Coreset (teal/green), BADGE (dark blue), Entropy (brown), Marg (orange), and Rand (purple). Across all plots accuracy improves rapidly early and then plateaus: for MLP with batch size 100 accuracy rises from roughly 0.25 to about 0.80 by ~30,000 labels; with batch size 1000 it rises from ~0.20 to ~0.82 by 50,000 labels; with batch size 10,000 there is a large initial jump (by ~10,000 labels) from ~0.20 to ~0.78 and then a slow climb approaching ~0.80–0.82. ResNet plots show higher peak accuracy: with batch 100 accuracy goes from ~0.10–0.20 up to ~0.90 by 50,000 labels; batch 1000 reaches ~0.88–0.90 by 50,000; batch 10,000 shows a steep jump to ~0.78–0.82 early and approaches ~0.88–0.90. VGG plots show similar high performance: batch 100 rises from ~0.20 to ~0.88–0.90 by ~25–30k labels; batch 1000 reaches ~0.90 by 50k; batch 10,000 jumps early to ~0.85–0.90 and then flattens. Curves for different active-learning methods largely track each other closely in all subplots, with minor method-to-method differences visible as slightly higher or lower traces.

Figure 10: Full learning curves for SVHN with MLP, ResNet and VGG.

Image /page/15/Figure/3 description: Three side-by-side line plots comparing active learning methods on MNIST using an MLP, with different batch sizes. Each subplot has the title at top: left "MNIST, MLP, Batch size: 100", center "MNIST, MLP, Batch size: 1000", right "MNIST, MLP, Batch size: 10000". The x-axis of every subplot is labeled "#Labels queried" (plotted from 0 up to 50,000 with tick marks around 10,000, 20,000, etc.), and the y-axis is labeled "Accuracy" (range shown roughly from 0.65 up to 0.96). Light gridlines are visible across all plots. Each plot shows multiple colored lines corresponding to different query strategies; a single legend below all three plots lists the methods and their colors: ALBL (light blue), Conf (yellow), Coreset (green), BADGE (darker blue/teal), Entropy (tan/light brown), Marg (orange), Rand (lavender/pink). Numerically, all methods start at around 0.65–0.75 accuracy with few labels and quickly rise as more labels are queried; by ~10,000–20,000 labels most curves have reached about 0.95 accuracy and then plateau near 0.95–0.96 for larger label counts. The rightmost subplot (batch size 10,000) shows a steep jump between the first and second plotted label amounts (from ~0.70 to ~0.95) then all methods remain tightly clustered near the top accuracy (~0.95–0.96). Overall the curves for all methods are close to one another, converging near 0.95–0.96 accuracy as label count increases.

Figure 11: Full learning curves for MNIST with MLP.

Image /page/16/Figure/1 description: A 3x3 grid of line plots comparing active learning methods on CIFAR-10 for three model architectures (rows) and three batch sizes (columns). Each subplot title names the setting: top row "CIFAR10, MLP, Batch size: 100", "CIFAR10, MLP, Batch size: 1000", "CIFAR10, MLP, Batch size: 10000"; middle row "CIFAR10, ResNet, Batch size: 100", "CIFAR10, ResNet, Batch size: 1000", "CIFAR10, ResNet, Batch size: 10000"; bottom row "CIFAR10, VGG, Batch size: 100", "CIFAR10, VGG, Batch size: 1000", "CIFAR10, VGG, Batch size: 10000". All subplots share the x-axis labeled "#Labels queried" (range ticks visible from a few thousand up to 40,000–50,000 depending on panel) and y-axis labeled "Accuracy". Each plot shows multiple colored lines (seven methods) that rise as more labels are queried: methods in the legend are ALBL, Conf, Coreset, BADGE, Entropy, Marg, and Rand (distinct colors including light blue, gold, green, dark blue, brown, orange, and pink/purple). Approximate numeric behavior: MLP panels start near ~0.25 accuracy and monotonically increase to about 0.45–0.50; ResNet panels start lower (~0.10–0.20) and rise to ~0.55–0.60; VGG panels start around ~0.15–0.30 and increase to ~0.75–0.80. For the largest batch size columns there is a sharp early jump in accuracy around the first 5,000–15,000 labels followed by gradual gains. A shared legend spanning the bottom identifies the seven methods and their colors.

Figure 12: Full learning curves for CIFAR10 with MLP, ResNet and VGG.

Image /page/16/Figure/3 description: Three side-by-side line plots comparing active learning methods on OpenML#6 using an MLP, with different batch sizes. Each subplot has y-axis labeled "Accuracy" (range ~0.40–0.95) and x-axis labeled "#Labels queried." Left plot title: "OpenML#6, MLP, Batch size: 100"; middle: "OpenML#6, MLP, Batch size: 1000"; right: "OpenML#6, MLP, Batch size: 10000." A legend below the plots lists seven methods with colors: ALBL (light blue), Conf (gold/yellow), Coreset (teal), BADGE (dark blue), Entropy (tan/light brown), Marg (orange), Rand (purple). In the left and middle plots most curves start around 0.45–0.55 accuracy at small numbers of labels, rapidly rise to about 0.85–0.90 within the first few thousand queried labels, and then plateau near ~0.92–0.95 as more labels are added. In the right plot all methods show a near-linear increase from about 0.45 accuracy at low label counts to roughly 0.90–0.93 at 10,000 queried labels, with the curves closely overlapping. Gridlines are shown in each subplot.

Figure 13: Zoomed-in learning curves for OpenML #6 with MLP.

Image /page/17/Figure/1 description: A three-panel line-plot comparison of active learning strategies for an MLP on OpenML#155, with a shared legend below. Each panel title reads, from left to right: "OpenML#155, MLP, Batch size: 100", "OpenML#155, MLP, Batch size: 1000", and "OpenML#155, MLP, Batch size: 10000". The y-axis on all panels is labeled "Accuracy" and ranges from 0.60 (or 0.65) up to 1.00 with horizontal gridlines. The x-axis on the left panel is labeled "#Labels queried" with ticks approximately 0, 1000, 2000, … up to ~7000; the middle panel x-axis ticks run roughly 0, 1000, 2000, … up to ~9000; the right panel x-axis ticks run 0, 10000, 20000, 30000, 40000, 50000. Each panel shows multiple colored curves that rise from lower accuracy to near-perfect accuracy as more labels are queried. The legend (centered below the plots) identifies the methods and their colors: ALBL (light blue), Conf (yellow), Coreset (green), BADGE (darker blue), Entropy (tan/brown), Marg (orange), and Rand (pink/lilac). In the batch size 100 panel, accuracies start around 0.65–0.75 and most methods steadily climb and converge near 0.98–1.00 by ~7000 labels. In the batch size 1000 panel, curves start slightly lower (≈0.60–0.70) and approach ≈0.98–1.00 by ~9000 labels. In the batch size 10000 panel, there is a large early jump: accuracy rises from about 0.65 to ~0.95–0.98 by ~10000 labels and then plateaus near 0.99–1.00 across methods by 50000 labels. Overall the methods behave similarly at high label counts, with some small early differences (e.g., Conf and Entropy sometimes lagging slightly) shown by the colored lines.

Figure 14: Zoomed-in learning curves for OpenML #155 with MLP.

Image /page/17/Figure/3 description: A three-panel figure of line plots comparing active learning strategies on the dataset OpenML#156 using an MLP. Each panel has y-axis labeled “Accuracy” (range approximately 0.65–0.90) and x-axis labeled “#Labels queried.” Titles across the top of the panels read (left) “OpenML#156, MLP, Batch size: 100”, (center) “OpenML#156, MLP, Batch size: 1000”, and (right) “OpenML#156, MLP, Batch size: 10000”. Each panel shows multiple colored lines with translucent shaded bands (error/confidence intervals) for the methods: ALBL (light blue), Conf (yellow), Coreset (green), BADGE (dark blue), Entropy (light brown/tan), Marg (orange), and Rand (purple). Common patterns: at very low numbers of queried labels accuracy starts near ~0.68–0.70, then rises steeply. Left panel (batch 100): most methods climb toward ~0.88–0.90 by roughly 1,000–3,000 labels, while Coreset (green) remains lower, around 0.75–0.82 across the range. Center panel (batch 1000): methods reach ~0.88–0.90 by ~1,500–3,000 labels, with greater spread and Coreset again lagging near ~0.75–0.82. Right panel (batch 10000): all methods rapidly reach and plateau near ~0.90 by ~10,000–15,000 labels. A legend centered below the plots identifies the seven methods and matches their line colors.

Figure 15: Zoomed-in learning curves for OpenML #156 with MLP.

Image /page/17/Figure/5 description: A three-panel line-plot figure comparing active learning methods on OpenML#184 using an MLP, with different batch sizes. Each panel plots Accuracy (y-axis, range about 0.20–0.80 with ticks at roughly 0.20, 0.30, 0.40, 0.50, 0.60, 0.70, 0.80) versus #Labels queried (x-axis). Left panel title: "OpenML#184, MLP, Batch size: 100" with x-axis ticks up to 22,000 (shown at ~0, 5,000, 10,000, 15,000, 20,000). Middle panel title: "OpenML#184, MLP, Batch size: 1000" with similar x range and ticks. Right panel title: "OpenML#184, MLP, Batch size: 10000" with x-axis from 0 to 20,000 (ticks at ~2,500, 5,000, 7,500, 10,000, 12,500, 15,000, 17,500, 20,000). In all panels multiple colored lines show seven methods (legend below): ALBL (light blue), Conf (yellow), Coreset (teal/green), BADGE (darker blue), Entropy (tan/brown), Marg (orange), Rand (pink/purple). General trend: all methods start around accuracy ~0.25–0.35 at few thousand labels and increase monotonically toward ~0.78–0.81 as more labels are queried. For batch size 100 and 1000 the curves gradually rise and converge near 0.80 by ~20,000 labels; for batch size 10,000 the accuracy increases roughly linearly from ~0.28 to ~0.75 by ~10,000 labels and then to ~0.80 by 20,000. Individual method lines are closely clustered with small shaded confidence bands around them.

Figure 16: Zoomed-in learning curves for OpenML #184 with MLP.

Image /page/18/Figure/1 description: A 3x3 panel figure of line plots reporting active learning accuracy on the SVHN dataset for three network architectures (rows) and three labeling batch sizes (columns). Each subplot title (top row) reads: "SVHN, MLP, Batch size: 100", "SVHN, MLP, Batch size: 1000", "SVHN, MLP, Batch size: 10000"; middle row: "SVHN, ResNet, Batch size: 100", "SVHN, ResNet, Batch size: 1000", "SVHN, ResNet, Batch size: 10000"; bottom row: "SVHN, VGG, Batch size: 100", "SVHN, VGG, Batch size: 1000", "SVHN, VGG, Batch size: 10000". X-axes are labeled "#Labels queried" and show tick ranges appropriate to batch size (examples: left-column plots show ticks at 2500, 5000, … up to 20000–35000; middle-column up to ~28000–40000; right-column up to 50000). Y-axes are labeled "Accuracy" and show ranges 0.20–0.80 for MLP plots and roughly 0.10–0.90 for ResNet and VGG plots.

Each subplot overlays multiple colored method curves comparing active learning strategies: ALBL (light blue), Conf (yellow/orange), Coreset (green), BADGE (darker blue/teal), Entropy (brown/amber), Marg (orange-brown), and Rand (pink/purple) — a legend with these labels appears along the bottom of the figure. Curves generally rise quickly from low accuracy at small numbers of queried labels (around 0.2 for MLP/VGG/ResNet initial points) and plateau as more labels are acquired. Typical numeric outcomes: MLP models converge near 0.78–0.82 accuracy, while ResNet and VGG converge higher near ~0.88–0.91. For batch size 100 the curves are more gradual and show more spread/noise; for batch size 1000 they rise faster and are closer together; for batch size 10000 nearly all methods jump early (by ~10000 labels) and plateau with very similar performance.

Overall the figure demonstrates comparable final accuracies across methods with architecture-dependent ceiling (MLP lower, ResNet/VGG higher) and decreasing method difference as batch size increases.

Figure 17: Zoomed-in learning curves for SVHN with MLP, ResNet and VGG.

Image /page/18/Figure/3 description: A three-panel line plot comparing active learning strategies on MNIST with an MLP. Each panel has y-axis labeled "Accuracy" and x-axis labeled "#Labels queried" and contains multiple colored accuracy-vs-labels curves with gridlines. Panel titles (left to right): "MNIST, MLP, Batch size: 100", "MNIST, MLP, Batch size: 1000", "MNIST, MLP, Batch size: 10000". Left plot x-axis range shown approximately 0–22,500 with tick labels at ~2500, 5000, 7500, 10000, 12500, 15000, 17500, 20000, 22500; middle plot x-axis to ~20,000 with similar ticks; right plot x-axis to 50,000 with ticks at 10,000, 20,000, 30,000, 40,000, 50,000. Y-axis ranges roughly 0.65–0.97 (ticks shown ~0.70, 0.75, 0.80, 0.85, 0.90, 0.95). Curves plotted correspond to seven strategies (legend centered below the three plots): ALBL (light blue), Conf (yellow/orange), Coreset (green-teal), BADGE (dark blue), Entropy (brown/tan), Marg (orange-brown), Rand (pink/purple). Common trend: all methods start around 0.70–0.75 accuracy at very few labels, rise quickly as more labels are queried, and converge to about 0.95–0.97 accuracy. In the rightmost (batch size 10000) panel there is a steep jump to ≈0.95–0.97 by ~10,000 labels and curves then plateau. In the smaller-batch panels the rise is more gradual but all methods reach similar final accuracy. The figure emphasizes comparative trajectories of the seven acquisition methods rather than precise numeric differences between them.

Figure 18: Zoomed-in learning curves for MNIST with MLP.

Image /page/19/Figure/1 description: A 3x3 grid of line plots showing active learning accuracy on CIFAR10 for three model architectures (rows) and three batch sizes (columns). Column titles across the top read: "Batch size: 100", "Batch size: 1000", "Batch size: 10000" (each preceded by the dataset and model in the subplot title). Row titles (part of subplot titles) are: top row "CIFAR10, MLP", middle row "CIFAR10, ResNet", bottom row "CIFAR10, VGG". Each subplot has x-axis labeled "#Labels queried" and y-axis labeled "Accuracy" with gridlines. Nine plots show multiple colored method curves that track test accuracy as more labels are queried: light blue (ALBL), gold/yellow (Conf), green (Coreset), darker blue (BADGE), brown (Entropy), orange (Marg), and purple (Rand) as indicated in the legend beneath the grid. Numeric ranges and trends: for MLP (top row) accuracy starts near ~0.25 and rises to about 0.47–0.50 for batch size 100 and 1000, and shows a steep early jump to ~0.45 then slowly to ~0.50 for batch size 10000. For ResNet (middle row) accuracy starts lower (~0.10–0.25 depending on batch) and climbs to roughly 0.55–0.60 by the largest label counts (best curves near 0.58–0.60); the batch-10000 plots show a sharp early increase around the first 10k labels then gradual growth to ~0.60. For VGG (bottom row) accuracy reaches the highest values: starting ~0.10–0.20 and rising to about 0.78–0.82 at the largest numbers of labels; batch size 10000 again shows a big early jump and then plateaus near ~0.80. Overall the colored methods largely overlap with small differences; no method dominates clearly across all setups. Axes ticks indicate label counts up to ~40,000–45,000 on the x-axis and accuracy ticks from about 0.10 to 0.80 on the y-axes depending on row.

Figure 19: Zoomed-in learning curves for CIFAR10 with MLP, ResNet and VGG.

Image /page/20/Figure/1 description: The figure shows three side-by-side square heatmaps comparing pairwise numeric differences between seven active-learning acquisition methods. Each heatmap has the same row/column labels (BADGE, ALBL, Coreset, Conf, Marg, Entropy, Rand) with zeroes on the diagonal and a colorbar to the right of each plot. The three titles (above each heatmap) are: “Batch size: 100(11)”, “Batch size: 1000(11)”, and “Batch size: 10000(11)”. The color scales shrink from left to right (left heatmap up to about 5.8, middle up to about 4.9, right up to about 2.8). Numeric values visible in each cell are as follows (rows = method on left, columns = method on top). All diagonal entries are 0.0.

Left heatmap (Batch size: 100(11)):
- BADGE: [0.0, 4.44, 5.35, 5.36, 0.76, 5.81, 4.09]
- ALBL: [0.2, 0.0, 2.71, 1.80, 0.31, 3.19, 1.49]
- Coreset:[0.2, 0.34, 0.0, 2.71, 0.87, 3.28, 1.76]
- Conf: [0.21, 0.67, 3.02, 0.0, 2.08, 2.18, 2.18]
- Marg: [0.46, 3.90, 4.77, 4.82, 0.0, 5.25, 3.77]
- Entropy:[0.31, 0.59, 2.60, 0.33, 0.10, 0.0, 1.58]
- Rand: [0.20, 2.26, 3.53, 3.45, 0.30, 4.53, 0.0]
Bottom (aggregate) row under this heatmap: [0.23, 1.74, 3.14, 2.64, 0.33, 3.45, 2.12]

Middle heatmap (Batch size: 1000(11)):
- BADGE: [0.0, 3.58, 3.96, 4.37, 1.96, 4.85, 4.37]
- ALBL: [0.14, 0.0, 2.14, 1.02, 2.12, 2.20, 2.20]
- Coreset:[0.62, 1.19, 0.0, 2.00, 1.04, 3.02, 2.80]
- Conf: [0.00, 1.12, 3.26, 0.0, 2.71, 2.65, 2.65]
- Marg: [0.17, 2.24, 4.10, 3.51, 0.0, 4.58, 3.99]
- Entropy:[0.00, 0.62, 2.45, 0.33, 0.00, 0.0, 1.95]
- Rand: [0.14, 2.20, 2.50, 3.05, 1.84, 3.95, 0.0]
Bottom (aggregate) row: [0.15, 1.56, 2.63, 2.04, 0.69, 3.03, 2.57]

Right heatmap (Batch size: 10000(11)):
- BADGE: [0.0, 1.17, 1.67, 2.83, 1.17, 2.50, 1.58]
- ALBL: [0.00, 0.0, 0.33, 0.33, 0.00, 1.50, 1.25]
- Coreset:[0.83, 0.50, 0.0, 1.83, 1.17, 1.83, 2.00]
- Conf: [0.33, 1.00, 1.33, 0.0, 0.33, 1.17, 1.25]
- Marg: [0.33, 1.00, 2.00, 1.00, 0.0, 1.50, 1.58]
- Entropy:[0.50, 1.17, 1.58, 2.17, 0.83, 0.0, 2.17]
- Rand: [0.29, 0.77, 1.13, 1.25, 0.54, 1.52, 1.32]
Bottom (aggregate) row: [0.29, 0.77, 1.13, 1.25, 0.54, 1.52, 1.32]

Notable patterns visible: the leftmost heatmap (smallest batch size 100) has the largest pairwise differences (many values >4, e.g., BADGE vs Entropy 5.81, BADGE vs Coreset 5.35, Marg vs Entropy 5.25). The middle heatmap (batch size 1000) shows somewhat smaller but still large differences (e.g., BADGE vs Entropy 4.85, Marg vs Entropy 4.58). The rightmost heatmap (batch size 10000) shows overall much smaller differences (largest values ~2.8 for BADGE vs Conf). Each heatmap uses the same ordering of methods across rows and columns, and includes a vertical colorbar indicating the numeric scale to the right of each plot.

Figure 20: Pairwise penalty matrices of the algorithms, grouped by different batch sizes. The parenthesized number in the title is the total number of (*D, B, A*) combinations aggregated, which is also an upper bound on all its entries. Element (*i, j*) corresponds roughly to the number of times algorithm *i* beats algorithm *j*. Column-wise averages at the bottom show aggregate performance (lower is better). From left to right: batch size = 100, 1000, 10000.

Image /page/20/Figure/3 description: The figure is a row of three small square heatmaps comparing active-learning methods by model. Each heatmap is titled above: left: “MLP(21)”, center: “ResNet(6)”, right: “VGG(6)”. Both the rows (y) and columns (x) of every heatmap use the same ordered method labels: BADGE, ALBL, Coreset, Conf, Marg, Entropy, Rand. A shared vertical label at the far left reads “Accuracy”. Each cell contains a numeric value and is colored by a vertical colorbar at the right of each heatmap (darker/purple = low, yellow/green = high). Numeric ranges on the colorbars differ by model: MLP spans approximately 0–11, ResNet ~0–1.7, VGG ~0–1.6. Example numeric values visible in the MLP(21) heatmap: BADGE vs BADGE = 0.0, BADGE vs ALBL = 8.31, BADGE vs Coreset = 10.67, BADGE vs Conf = 11.0, BADGE vs Marg = 2.77, BADGE vs Entropy = 10.94, BADGE vs Rand = 8.39. Other MLP cells include ALBL vs Coreset = 5.18, ALBL vs Conf = 2.33, Coreset vs Conf = 5.32, Marg vs Entropy = 10.34, and a bottom summary row of values (across columns) of about [0.37, 3.59, 6.54, 4.92, 0.97, 6.61, 4.51]. Example numeric values in ResNet(6): BADGE row [0.0, 0.29, 0.3, 1.03, 0.82, 1.3, 0.64]; ALBL has values such as 0.34 and 0.0 on the diagonal; Coreset vs Marg ≈1.28; Rand vs Entropy ≈1.75; the ResNet bottom summary row is shown as about [0.11, 0.19, 0.26, 0.72, 0.43, 0.83, 0.57]. Example numeric values in VGG(6): BADGE row [0.0, 0.59, 0.0, 0.53, 0.29, 0.92, 1.02]; ALBL vs Rand ≈1.3; Coreset vs Rand ≈1.69; Rand vs Entropy ≈0.98; the VGG bottom summary row is about [0.19, 0.3, 0.1, 0.3, 0.16, 0.57, 0.94]. Overall, the three heatmaps show pairwise numeric comparisons between the seven methods for each model, with higher values (yellow/green) concentrated in different method pairs depending on the model (most extreme values in the MLP heatmap).

Figure 21: Pairwise penalty matrices of the algorithms, grouped by different neural network models. The parenthesized number in the title is the total number of (*D, B, A*) combinations aggregated, which is also an upper bound on all its entries. Element (*i, j*) corresponds roughly to the number of times algorithm *i* beats algorithm *j*. Column-wise averages at the bottom show aggregate performance (lower is better). From left to right: MLP, ResNet and VGG.

total number of (*D, B, A*) combinations aggregated; as discussed in Section 4, this is also an upper bound on all its entries. It can be seen that uncertainty-based methods (e.g. MARG) perform well only in small batch size regimes (100) or when using MLP models; representative sampling based methods (e.g. CORESET) only perform well in large batch size regimes (10000) or when using ResNet or VGG models. In contrast, BADGE's performance is competitive across all batch sizes and neural network models.

Image /page/21/Figure/1 description: A wide figure containing three side-by-side cumulative distribution (step) plots comparing normalized error across several selection strategies. Each subplot has the y-axis labeled "Cumulative frequency" (0.0 to 1.0) and the x-axis labeled "Normalized error" (range shown from 0.3 to 1.2). The three subplot titles (left to right) are: "Batch size: 100", "Batch size: 1000", and "Batch size: 10000". Each plot shows multiple stepped CDF lines in different colors (and a vertical purple reference line near x = 1.0): ALBL (light cyan), Conf (yellow/gold), Coreset (teal/green), BADGE (blue), Entropy (tan/beige), Marg (orange), and Rand (purple vertical line). All plots use faint gridlines. In the left plot (Batch size: 100) the curves begin rising roughly between normalized error ~0.8–1.0 and reach high cumulative values near 1.1–1.2. In the middle plot (Batch size: 1000) the curves are slightly more concentrated and rise mainly between ~0.85 and ~1.05. In the right plot (Batch size: 10000) the step increases are steeper and concentrated roughly between ~0.9 and ~1.05, with most methods reaching high cumulative frequency just above 1.0. A long legend centered below the plots lists the methods and their corresponding colors in the order: ALBL, Conf, Coreset, BADGE, Entropy, Marg, Rand.

Figure 22: CDFs of normalized errors of the algorithms, group by different batch sizes. Higher CDF indicates better performance. From left to right: batch size = 100, 1000, 10000.

Image /page/21/Figure/3 description: A three-panel figure of cumulative distribution plots comparing normalized error for several active-learning methods across three model architectures. Panels are labeled above each plot: left "MLP", center "ResNet", and right "VGG". Each panel has the x-axis labeled "Normalized error" ranging from 0.3 to 1.2 and the y-axis labeled "Cumulative frequency" from 0.0 to 1.0. Multiple colored step-style curves appear in each panel for different methods; a vertical magenta/purple line is drawn near x = 1.0 in every panel. A legend below the plots identifies the methods and their colors: ALBL (light cyan), Conf (yellow), Coreset (teal/green), BADGE (darker blue), Entropy (light brown), Marg (orange), and Rand (magenta vertical line). In the MLP panel the curves are more spread out, rising gradually between roughly 0.4 and 1.15 and reaching cumulative frequency = 1.0 near 1.1–1.2. In the ResNet panel the curves are tightly clustered, mostly starting near 0.7–0.8 and climbing sharply between about 0.9 and 1.05 to reach 1.0. In the VGG panel the curves lie between the other two: they start near 0.4–0.7 and mostly increase between ~0.8 and 1.1, reaching 1.0 by about 1.1–1.2.

Figure 23: CDFs of normalized errors of the algorithms, group by different neural network models. Higher CDF indicates better performance. From left to right: MLP, ResNet and VGG.

## E CDFS OF NORMALIZED ERRORS OF DIFFERENT ALGORITHMS

In addition to Figure 5 that aggregates over all settings, we show here the CDFs of normalized errors by conditioning on fixed batch sizes (100, 1000 and 10000) in Figure 22, and show the CDFs of normalized errors by conditioning on fixed neural network models (MLP, ResNet and VGG) in Figure 23.

## F BATCH UNCERTAINTY AND DIVERSITY

Figure 24 gives a comparison of sampling methods with gradient embedding in two settings (OpenML # 6, MLP, batchsize 100 and SVHN, ResNet, batchsize 1000), in terms of uncertainty and diversity of examples selected within batches. These two properties are measured by average `<sup>2</sup> norm and determinant of the Gram matrix of gradient embedding, respectively. It can be seen that, *k*-MEANS++ (BADGE) induces good batch diversity in both settings. CONF generally selects examples with high uncertainty, but in some iterations of OpenML #6, the batch diversity is relatively low, as evidenced by the corresponding log Gram determinant being 1. These areas are indicated by gaps in the learning curve for CONF. Situations where there are

Image /page/22/Figure/1 description: Figure composed of four subplots arranged 2x2 comparing selection methods (legend at bottom: k-DPP gray, k-means++ blue, Rand purple, FF k-center light blue, Conf orange). (a) Top-left: "OpenML #6, MLP, Batch size: 100". Y-axis labeled "Log determinant of batch" (range plotted roughly +500 down to -1,300), x-axis "#Labels queried" from 0 to 14,000. Multiple colored curves start near +400–+500 and steadily decline as more labels are queried; by 14,000 values lie around -700 to -1,200 with k-means++ (blue) and FF k-center (light blue) often producing lower (more negative) log-determinants than Rand (purple) and Conf (orange). (b) Top-right: "SVHN, ResNet, Batch size: 1000". Y-axis "Log determinant of batch" (range shown roughly 0 down to -35,000), x-axis "#Labels queried" 0–50,000. Curves start near 0, then decline drastically; k-means++ (blue) drops to about -33,000 around 15,000 queries then partially recovers to around -28,000, while other methods (gray k-DPP, orange Conf, light-blue FF k-center) remain in the -10k to -20k range; Rand (purple) stays relatively higher (less negative) around -15k to -25k. (c) Bottom-left: "OpenML #6, MLP, Batch size: 100". Y-axis "Average l2 norm in batch" (0–50), x-axis 0–14,000. Curves show average L2 norms that start between ~10 and ~45, then decay toward small values near 1–5 as queries increase. Conf (orange) peaks near 45 early, k-means++ (blue) near 30, k-DPP (gray) ~25, FF k-center (light blue) ~20, Rand (purple) lower (~10); all approach ~1–3 by the right side. (d) Bottom-right: "SVHN, ResNet, Batch size: 1000". Y-axis "Average l2 norm in batch" (0–11), x-axis 0–50,000. Curves peak between 5 and 11 around 8,000–12,000 labels (Conf orange peaks near 11, k-means++ blue ~8–9, k-DPP gray ~6), then fall to near zero or ~1 by ~20,000–30,000 queries. Overall the figure contrasts log-determinant and average L2-norm trajectories across five selection strategies on two datasets/models (OpenML #6 with an MLP, and SVHN with a ResNet) and two batch sizes (100 and 1000).

Figure 24: A comparison of batch selection algorithms in gradient space. Plots a and b show the log determinants of the Gram matrices of gradient embeddings within batches as learning progresses. Plots c and d show the average embedding magnitude (a measurement of predictive uncertainty) in the selected batch. The *k*-centers sampler finds points that are not as diverse or high-magnitude as other samplers. Notice also that *k*-MEANS++ tends to actually select samples that are both more diverse and higher-magnitude than a *k*-DPP, a potential pathology of the *k*-DPP's degree of stochastisity. Among all algorithms, CONF has the largest average norm of gradient embeddings within a batch; however, in OpenML #6, and the first few interations of SVHN, some batches have a log Gram determinant of 1 (shown as gaps in the curve), which shows that CONF sometimes selects batches that are inferior in diversity.

many gaps in the CONF plot seem to correspond to situations in which CONF performs poorly in terms of accuracy (see Figure 13 for the corresponding learning curve). Both *k*-DPP and FF-*k*-CENTER (an algorithm that approximately minimizes *k*-center objective) select batches that have lower diversity than *k*-MEANS++ (BADGE).

## G COMPARISON OF *k*-MEANS++ AND *k*-DPP IN BATCH SELECTION

In Figures 25 to 31, we give running time and test accuracy comparisons between *k*-MEANS++ and *k*-DPP for selecting examples based on gradient embedding in batch mode active learning. We implement the *k*-DPP sampling using the MCMC algorithm from (Kang, 2013), which has a time complexity of *<sup>O</sup>*(⌧ *·* (*k*<sup>2</sup> <sup>+</sup> *kd*))

Image /page/23/Figure/1 description: A four-panel experimental plot comparing two selection methods (gray = k-DPP, blue = k-means++) on OpenML dataset #6 using an MLP. From left to right: (1) Accuracy vs #Labels queried for batch size 100: y-axis labeled Accuracy (range ~0.40–0.92). Both methods rise quickly from ~0.40 at few labels to about 0.90–0.92 by ~2,000 labels and then plateau; the blue k-means++ curve is slightly above the gray k-DPP curve. Shaded bands around each line show confidence intervals. X-axis ticks run from 0 to ~16,000 (#Labels queried). (2) Time vs #Labels queried for batch size 100: y-axis labeled Time with a multiplier note “×10^4”. The gray k-DPP time increases approximately linearly up to about 2.2 (×10^4) while the blue k-means++ time stays much lower (around 0.2–0.6 ×10^4). Shaded variance bands shown. (3) Accuracy vs #Labels queried for batch size 1000: similar to panel 1, accuracy climbs rapidly to ~0.90–0.92 by a few thousand labels and plateaus; k-means++ (blue) slightly outperforms k-DPP (gray). (4) Time vs #Labels queried for batch size 1000: y-axis labeled Time with multiplier “×10^5”. k-DPP time grows roughly linearly up to about 2.0 (×10^5) while k-means++ remains far lower (near 0.2–0.3 ×10^5). A centered legend below the plots shows a gray line labeled “k-DPP” and a blue line labeled “k-means++”. Gridlines are visible in all panels.

Figure 25: Learning curves and running times for OpenML #6 with MLP.

Image /page/23/Figure/3 description: A 1x4 panel figure comparing two selection methods (k-DPP in gray and k-means++ in blue) on OpenML dataset #155 using an MLP. The four small subplots are arranged left-to-right: (1) Accuracy (batch size: 100) – title "OpenML#155, MLP, Batch size: 100"; x-axis labeled "#Labels queried" (ticks shown at 0, 10000, 20000, 30000, 40000, ~50000) and y-axis labeled "Accuracy" spanning 0.60 to 1.00. Two curves with shaded confidence bands quickly rise from ~0.60–0.70 and plateau near ~0.98 accuracy within the first few thousand labels; the gray (k-DPP) and blue (k-means++) curves are nearly overlapping. (2) Time (batch size: 100) – same title; x-axis "#Labels queried" (same range) and y-axis labeled "Time" (vertical ticks roughly 0, 2, 4, 6 with a multiplier annotation shown on the plot). The gray k-DPP curve grows roughly linearly with a larger slope and wider confidence band; the blue k-means++ curve also increases but with a smaller slope and narrower band, so k-DPP is noticeably slower. (3) Accuracy (batch size: 1000) – title "OpenML#155, MLP, Batch size: 1000"; axes same as panel 1. Both methods again rapidly reach a high accuracy (~0.95–0.99) and remain nearly identical with narrow confidence intervals. (4) Time (batch size: 1000) – same title as panel 3; x-axis "#Labels queried" and y-axis labeled "Time" (ticks shown ~0.0, 0.5, 1.0, 1.5, 2.0 with a multiplier annotation). The gray k-DPP time increases steeply with large uncertainty reaching the highest times of the figure, while the blue k-means++ time remains much lower and grows slowly. A centered legend below the panels shows a gray line labeled "k-DPP" and a blue line labeled "k-means++". Shaded bands around each curve indicate variability/confidence intervals.

Figure 26: Learning curves and running times for OpenML #155 with MLP.

and space complexity of *<sup>O</sup>*(*k*<sup>2</sup> <sup>+</sup> *kd*), where ⌧ is the number of sampling steps. We set ⌧ as <sup>b</sup>5*<sup>k</sup>* ln *<sup>k</sup>*<sup>c</sup> in our experiment. The comparisons for batch size 10000 are not shown here as the implementation of *k*-DPP sampling runs out of memory.

It can be seen from the figures that, although *k*-DPP and *k*-MEANS++ are based on different sampling criteria, the classification accuracies of their induced active learning algorithm are similar. In addition, when large batch sizes are required (e.g. *k* = 1000), the running times of *k*-DPP sampling are generally much higher than those of *k*-MEANS++.

Image /page/23/Figure/7 description: Figure composed of four small line plots arranged in a single row, comparing two selection methods on OpenML#156 with an MLP model. The two left panels are for batch size 100 and the two right panels are for batch size 1000. Each pair contains an accuracy plot (left of each pair) and a runtime plot (right of each pair). Common x-axis on all plots is “#Labels queried” with tick marks roughly at 0, 10,000, 20,000, 30,000, 40,000 (range up to ~50,000). Accuracy plots (y-axis labeled “Accuracy”) span from 0.65 to 0.90: both k-DPP (grey) and k-means++ (blue) rapidly rise and plateau near ~0.90 within the first few thousand labels, with almost identical final accuracies. Runtime plots (y-axis labeled “Time”) show separate panels whose titles include a “×10” multiplier: for batch size 100 the time axis reaches higher values (about 0–6 on the plotted scale) while for batch size 1000 the axis range is smaller (about 0–2 on the plotted scale). In the runtime plots the grey curve (k-DPP) grows much more steeply with #labels queried than the blue curve (k-means++), indicating substantially higher computational cost for k-DPP; k-means++ remains substantially faster, especially at larger batch size. All curves have semi-transparent shaded confidence bands. A centered legend below the plots labels the grey trace as “k-DPP” and the blue trace as “k-means++”. The main titles over each column read “OpenML#156, MLP, Batch size: 100” (left two) and “OpenML#156, MLP, Batch size: 1000” (right two).

Figure 27: Learning curves and running times for OpenML #156 with MLP.

Image /page/24/Figure/1 description: A four-panel figure comparing two selection methods (gray = k-DPP, blue = k-means++) on the OpenML#184 dataset using an MLP. Panels are arranged left-to-right: accuracy then runtime for batch size 100, then accuracy then runtime for batch size 1000. Common axes: x-axis "#Labels queried" from 0 to ≈14,000 (tick marks at 2000, 4000, ...). A centered legend beneath the panels shows a gray line labeled "k-DPP" and a blue line labeled "k-means++"; both lines include a translucent confidence/shaded band. Top-left panel: title "OpenML#184, MLP, Batch size: 100", y-axis "Accuracy" (≈0.25–0.80). Both methods show nearly identical rising accuracy curves, starting near ~0.28 and increasing monotonically to about 0.78–0.80 as labels approach 14,000. Top-right panel (runtime for batch size 100): title includes a leading "×10" and reads "OpenML#184, MLP, Batch size: 100"; y-axis "Time" from 0 to about 1.2. Time grows slowly with #labels; k-means++ (blue) is slightly faster (lower) than k-DPP (gray) but both remain under ~1.2. Bottom-left panel: title "OpenML#184, MLP, Batch size: 1000", y-axis "Accuracy" (≈0.30–0.80). Accuracy curves again nearly overlap and rise from roughly 0.35–0.40 up to about 0.78–0.80 by ≈14,000 labels, with a steeper initial gain. Bottom-right panel (runtime for batch size 1000): title includes "×10 OpenML#184, MLP, Batch size: 1000", y-axis "Time" up to about 8.0. Here k-DPP (gray) runtime grows much faster, reaching roughly 8 at the largest #labels, while k-means++ (blue) remains substantially lower (around ~2 at the largest #labels). Overall the figure shows nearly identical accuracy performance for both methods across label budgets, while k-means++ has considerably lower runtime, especially at the larger batch size.

Figure 28: Learning curves and running times for OpenML #184 with MLP.

Image /page/24/Figure/3 description: Composite figure with eight small line plots comparing two selection methods (gray: k-DPP, blue: k-means++) on the SVHN dataset. Layout: two rows (top = MLP, bottom = ResNet) and four columns (for each architecture there are two pairs of plots: left pair for batch size 100, right pair for batch size 1000). In each pair the left subplot shows Accuracy vs #Labels queried and the right subplot shows Time vs #Labels queried. All x-axes are labeled "#Labels queried" (ranges shown roughly 0 to 30,000 for MLP plots and 0 to 50,000 for ResNet plots). Accuracy y-axes range roughly from 0.10–0.90: accuracy curves start low (~0.2 for MLP, ~0.10–0.20 for ResNet) and rise quickly then asymptote between ~0.8 and ~0.9 as more labels are queried. Time plots show runtime growth with number of labels; several time plots include axis multipliers (top-middle and bottom-middle labeled x10^5, bottom-right labeled x10^4). Approximate time magnitudes: MLP batch size 100 reaches on the order of 3.0×10^5, MLP batch size 1000 up to ~1.2×10^5; ResNet batch size 100 up to ~2.0×10^5, ResNet batch size 1000 up to ~7×10^4. In all time plots the gray k-DPP curve is above the blue k-means++ curve (k-DPP slower) while both methods achieve similar accuracy curves; shaded bands around each line indicate variability/confidence intervals. A legend centered beneath the plots shows the gray line labeled "k-DPP" and the blue line labeled "k-means++".

Figure 29: Learning curves and running times for SVHN with MLP and ResNet.

Image /page/24/Figure/5 description: A four-panel comparative plot for active learning on MNIST using an MLP. Top row left and third panels show accuracy vs. number of labels queried; top row second and fourth panels show computation time vs. number of labels queried. Panel titles (left pair) read "MNIST, MLP, Batch size: 100" and (right pair) "MNIST, MLP, Batch size: 1000." X-axes on all panels are labeled "#Labels queried" and run from 0 to about 50,000. Accuracy plots (left and third) have a y-axis from about 0.70 to 0.95; both methods’ accuracy curves rise steeply from roughly 0.70 at low label counts to about 0.95 within the first few thousand labels and then plateau, with the two curves nearly overlapping. Time plots (second and fourth) have a y-axis annotated with a multiplier "x10^4" and run up to roughly 8×10^4; the time curves grow approximately linearly with #labels. A legend centered below the four panels shows two methods: a gray line labeled "k-DPP" and a blue line labeled "k-means++." For batch size 100 the blue k-means++ curve is visibly above the gray k-DPP curve (higher time), while for batch size 1000 the gray k-DPP curve is much higher than the blue k-means++ curve (k-DPP takes substantially more time).

Figure 30: Learning curves and running times for MNIST with MLP.

Image /page/25/Figure/1 description: A figure composed of eight small line plots (2 rows × 4 columns) comparing two selection methods on CIFAR10. Each column pair shows accuracy and runtime for two model types and batch sizes. Top row: “CIFAR10, MLP, Batch size: 100” (leftmost accuracy plot) and its corresponding “Time” plot (second from left, y-axis annotated with a multiplier ×10^5), then “CIFAR10, MLP, Batch size: 1000” (third: accuracy) and its time plot (fourth, annotated ×10^4). Bottom row: “CIFAR10, ResNet, Batch size: 100” (fifth: accuracy) and its time plot (sixth, ×10^5), then “CIFAR10, ResNet, Batch size: 1000” (seventh: accuracy) and its time plot (eighth, ×10^4). All x-axes are labeled “#Labels queried” with tick marks at approximately 0, 10,000, 20,000, 30,000, 40,000, 50,000. Accuracy y-axes range roughly 0.10–0.60 depending on subplot (MLP accuracy plots span ~0.25–0.50; ResNet plots span ~0.10–0.60). Each plot shows two curves with shaded confidence bands: a gray curve for “k-DPP” and a blue curve for “k-means++” (legend centered below the grid). General trends visible: accuracy for both methods rises quickly at small numbers of labels and then gradually plateaus by ~40–50k labels (k-means++ and k-DPP have similar accuracy trajectories, with small differences). Runtime curves show k-DPP (gray) is substantially more expensive and grows much faster than k-means++ (blue), especially for batch size 1000 (gray time reaching several ×10^4 while blue remains much lower). Shaded regions around lines indicate variability across runs.

Figure 31: Learning curves and running times for CIFAR10 with MLP and ResNet.