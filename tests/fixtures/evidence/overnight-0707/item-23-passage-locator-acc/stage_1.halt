{
  "status": "halted",
  "stage": "stage_1",
  "reason": "validate_paper_map.py failed with an UNCHANGED error signature after a fix dispatch (fixation \u2014 the fix did not move the error; iteration 2)",
  "retry_count": 2,
  "context": {
    "stderr": "normalized substring check failed). Quote equations EXACTLY as the paper writes them, LaTeX and delimiters included; remember every backslash in the JSON file must be doubled (\\\\theta, not \\theta). Got: '$$h_k = (\\\\mathbf{x}_k(1) - x_{obs})^2 + (\\\\mathbf{x}_k(2) - y_{obs})^2 - r_{obs}^2,$$ (17) \\\\nwhere $x_{obs}$ , $y_{obs}$ '\n  - eq-vehicle-dynamics: source_text is not a verbatim passage of the paper (whitespace-normalized substring check failed). Quote equations EXACTLY as the paper writes them, LaTeX and delimiters included; remember every backslash in the JSON file must be doubled (\\\\theta, not \\theta). Got: '$$\\\\mathbf{x}_{t+1} = f(\\\\mathbf{x}_t, \\\\mathbf{u}_t), \\\\tag{18}$$\\\\nwhere $x_t$ and $u_t$ represent the state and input of t'\n  - eq-racing-cost: source_text is not a verbatim passage of the paper (whitespace-normalized substring check failed). Quote equations EXACTLY as the paper writes them, LaTeX and delimiters included; remember every backslash in the JSON file must be doubled (\\\\theta, not \\theta). Got: '$$q(\\\\mathbf{x}_{t+k|t}, \\\\mathbf{u}_{t+k|t}) = (\\\\mathbf{x}_{t+k|t} - \\\\mathbf{x}_r)^T Q(\\\\mathbf{x}_{t+k|t} - \\\\mathbf{x}_r)'\n  - eq-barrier-quartic: source_text is not a verbatim passage of the paper (whitespace-normalized substring check failed). Quote equations EXACTLY as the paper writes them, LaTeX and delimiters included; remember every backslash in the JSON file must be doubled (\\\\theta, not \\theta). Got: '$$h_t^i = \\\\frac{(s_t - s_t^i)^4}{(2l_1)^4} + \\\\frac{(e_{y_t} - e_{y_t}^i)^4}{(2l_2)^4} - 1,$$ (21) where we assume all ra'\n  - eq-linearized-dynamics: source_text is not a verbatim passage of the paper (whitespace-normalized substring check failed). Quote equations EXACTLY as the paper writes them, LaTeX and delimiters included; remember every backslash in the JSON file must be doubled (\\\\theta, not \\theta). Got: '$$\\\\mathbf{x}_{t+1} = A\\\\mathbf{x}_t + B\\\\mathbf{u}_t,\\\\tag{22}$$\\\\nwhere A and B are calculated with a regression-based appr'\n"
  }
}
