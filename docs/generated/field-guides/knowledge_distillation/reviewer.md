<!-- Generated by scripts/refresh_ssot_docs.py taxonomy role views. -->
# knowledge_distillation - reviewer view

Source taxonomy node: `TE-TS/knowledge_distillation`
Legacy paradigm id: `knowledge_distillation`
Inheritance chain: knowledge_distillation

This generated view is maintainer documentation rendered from `docs/ssot/taxonomies.yaml`; runtime consumers should use `scripts.taxonomy` and `scripts.build_plan`.

## semantic_checks

```yaml
- id: KD-analyzer-teacher-provenance
  stage: stage_1_analyzer
  check: 'The spec records the teacher''s provenance: paper checkpoint, off-the-shelf
    model, or trained-from-scratch smoke substitute.

    '
  silent_failure: Downstream stages invent a teacher source and silently change the
    distillation target.
  severity: warning
  status: seed
- id: KD-seed-teacher-frozen
  stage: stage_2c_method
  check: The teacher is frozen (no grad updates to teacher params).
  silent_failure: Teacher params receive gradients; 'distillation' is just joint training.
  severity: error
  status: seed
- id: KD-arch-teacher-eval-frozen
  stage: stage_2b_architecture
  check: 'build_teacher returns a teacher in eval mode with every parameter frozen
    before the distillation loop can use it.

    '
  silent_failure: Batch-norm/dropout updates or gradients move the teacher while the
    method claims fixed-teacher KD.
  severity: error
  status: seed
- id: KD-method-loss-not-vanilla-fallback
  stage: stage_2c_method
  check: 'compute_distillation_loss implements the paper''s named KD components, not
    only vanilla softened-logit KL unless that is the paper''s actual contribution.

    '
  silent_failure: A complex feature/relation KD paper degenerates to Hinton-style
    KL and still runs.
  severity: error
  status: seed
```

## pluggable_component

```yaml
name: compute_distillation_loss
return_type: torch.Tensor
signature_template: compute_distillation_loss(student, teacher, batch, seed, *paradigm_extras_by_name)
  -> torch.Tensor
contract:
  fixed_positional_args:
  - student
  - teacher
  - batch
  seed_param: seed
  paradigm_extras_forwarding: by_name_from_config
  forbidden_param_names:
  - config
rationale: 'The notebook''s distillation loop calls the pluggable loss with fixed
  student, teacher, batch, and seed arguments; method-specific KD hyperparameters
  are forwarded by name from params.

  '
```

## build_plan

```yaml
schema_version: '1.0'
source: spec+taxonomy
interface_hint: compute_distillation_loss(student, teacher, batch, seed, *paradigm_extras_by_name)
  -> torch.Tensor
```

## package_manifest

```yaml
description: Knowledge-distillation package ownership.
files:
- path: method/data.py
  kind: paradigm_fixed
  produced_by: package_scaffolder
  public_symbols:
  - name: load_data
    kind: function
    signature: 'load_data(path: str | os.PathLike | None = None, *, train_size: int
      = 1000, n_test: int = 200, seed: int = 0) -> Tuple[torch.Tensor, torch.Tensor,
      torch.Tensor, torch.Tensor]'
- path: method/example_data/README.md
  kind: paradigm_fixed
  produced_by: package_scaffolder
- path: method/model.py
  kind: paradigm_shaped
  produced_by: architecture_coder
  public_symbols:
  - name: <StudentClass>
    kind: class
    inherits: torch.nn.Module
    required_methods:
    - 'forward(self, x: torch.Tensor) -> torch.Tensor'
  - name: <TeacherClass>
    kind: class
    inherits: torch.nn.Module
    required_methods:
    - 'forward(self, x: torch.Tensor) -> torch.Tensor'
- path: method/training.py
  kind: paradigm_fixed
  produced_by: architecture_coder
  public_symbols:
  - name: build_student
    kind: function
    signature: 'build_student(input_dim: int, n_classes: int, **arch_kwargs) -> <StudentClass>'
  - name: build_teacher
    kind: function
    signature: 'build_teacher(input_dim: int, n_classes: int, **arch_kwargs) -> <TeacherClass>'
  - name: train_with_distillation
    kind: function
    signature: 'train_with_distillation(student: <StudentClass>, teacher: <TeacherClass>,
      x_train: torch.Tensor, y_train: torch.Tensor, *, distillation_loss_fn: Callable,
      learning_rate: float, num_epochs: int, batch_size: int, seed: int, **distillation_kwargs)
      -> <StudentClass>'
- path: method/method.py
  kind: method_shaped
  produced_by: method_coder
  public_symbols:
  - name: compute_distillation_loss
    kind: function
    signature: compute_distillation_loss(student, teacher, batch, seed, *paradigm_extras_by_name)
      -> torch.Tensor
    contract:
      fixed_positional_args:
      - student
      - teacher
      - batch
      seed_param: seed
      paradigm_extras_forwarding: by_name_from_config
      forbidden_param_names:
      - config
  - name: <method_helpers>
    kind: function
    all_top_level_functions_are_public: true
- path: method/__init__.py
  kind: derived
  produced_by: init_finalizer
- path: requirements.txt
  kind: derived
  produced_by: init_finalizer
- path: method/README.md
  kind: paradigm_fixed
  produced_by: package_scaffolder
dependency_graph:
  method/data.py: []
  method/model.py: []
  method/method.py:
  - method/model.py
  method/training.py:
  - method/model.py
  - method/method.py
  method/__init__.py:
  - method/data.py
  - method/method.py
  - method/model.py
  - method/training.py
validators:
  after_package_scaffolder: scripts/validate_scaffolder_output.py
  after_architecture_coder: scripts/validate_architecture_coder_output.py
  after_method_coder: scripts/validate_method_coder_output.py
  after_init_finalizer:
  - scripts/validate_package_imports.py
  - scripts/validate_arch_contract.py
  - scripts/validate_arch_contract_runtime.py
```

## arch_contract_requirements

```yaml
required_blocks:
  data_loader.load_data_returns:
    type: dict[str, shape_string]
    min_keys:
    - x_train
    - y_train
    - x_test
    - y_test
  architecture.student:
    required_subblocks:
    - class_name
    - forward.input
    - forward.output_type
  architecture.teacher:
    required_subblocks:
    - class_name
    - forward.input
    - forward.output_type
  pluggable_component.name:
    must_equal: pluggable_component.name
  pluggable_component.batch_dict_shape:
    type: dict[str, shape_string]
    min_keys:
    - inputs
  training_loop.function_name:
    must_equal: train_with_distillation
  training_loop.input_shapes:
    type: dict[str, shape_string]
    min_keys:
    - x_train
    - y_train
```

## notebook_layout

```yaml
description: Section structure for knowledge-distillation notebooks.
sections:
- id: title
  title: Paper title (from spec.paper.title)
  kind: agnostic
- id: install
  title: '## 0. Install dependencies (first run only)'
  kind: agnostic
- id: setup
  title: '## 1. Setup'
  kind: agnostic
- id: parameters
  title: '## 2. Parameters'
  kind: agnostic
- id: setup_pieces
  title: '## 3. The setup pieces'
  kind: paradigm
  subsections:
  - id: data
    title: '### 3.1 Data'
  - id: teacher
    title: '### 3.2 Teacher model'
  - id: student
    title: '### 3.3 Student model'
  - id: verify_shapes
    title: '### 3.4 Architecture shape verification'
- id: method
  title: "## 4. The {method_name} method \u2B50"
  kind: method_specific
- id: running
  title: '## 5. Running distillation end-to-end'
  kind: paradigm
  subsections:
  - id: distill_loop
    title: '### 5.1 Distillation training loop'
  - id: before_after
    title: '### 5.2 Before/after comparison'
  - id: loss_decomposition
    title: '### 5.3 Distillation loss decomposition (single batch)'
- id: own_data
  title: '## 6. Use your own data'
  kind: agnostic
```
