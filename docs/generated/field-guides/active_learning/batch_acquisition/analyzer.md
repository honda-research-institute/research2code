<!-- Generated by scripts/refresh_ssot_docs.py taxonomy role views. -->
# active_learning/batch_acquisition - analyzer view

Source taxonomy node: `TE-TS/active_learning/batch_acquisition`
Legacy paradigm id: `active_learning/batch_acquisition`
Inheritance chain: active_learning -> active_learning/batch_acquisition

This generated view is maintainer documentation rendered from `docs/ssot/taxonomies.yaml`; runtime consumers should use `scripts.taxonomy` and `scripts.build_plan`.

## fingerprint

```yaml
what_it_is: "Selects a batch of B>1 examples per round, balancing uncertainty against\
  \ diversity so the batch is not B near-duplicate uncertain points (the failure na\xEF\
  ve top-K-uncertainty falls into).\n"
positive_signals:
- an acquisition function scoring unlabeled points
- a label budget with successive query rounds
- 'a learning curve: test accuracy vs. number of labels'
route_elsewhere:
- signal: selection is fixed/random, no model-in-the-loop
  to: TE-TS/semi_supervised_learning
- signal: the contribution is the loss/objective, not the selection rule
  to: TE-LF/task_specific_loss
worked_examples:
- BADGE (Ash et al. 2020)
- CORESET (Sener & Savarese 2018)
```

## priors

```yaml
architecture:
  for_image_data:
  - resnet18
  - vgg11
  - mlp
  for_tabular_data:
  - mlp
  flag_substitutions: true
seeds:
  minimum_for_ranking_claim: 3
  recommended_for_publication: 5
calibration:
  preserve:
  - batch_size
benchmark_selection: 'Prefer the smallest standard benchmark for data_setup (MNIST
  < OpenML < SVHN < CIFAR-10 < ...); all four numeric fields come from the same benchmark
  configuration. See content file.

  '
batch_size:
  conventional_values:
  - 100
  - 1000
  - 10000
  minimum_for_meaningful_test: 50
```

## scaffold_hints

```yaml
interface_hint: select_batch(model, x_unlabeled, batch_size, seed, *paradigm_extras_by_name)
  -> List[int]
content_file: docs/ssot/content/active_learning.md
templates_dir: paradigms/supervised_ml/active_learning/templates
demo_data_source: template_downloads
```

## pluggable_component

```yaml
name: select_batch
return_type: List[int]
signature_template: select_batch(model, x_unlabeled, batch_size, seed, *paradigm_extras_by_name)
  -> List[int]
contract:
  fixed_positional_args:
  - model
  - x_unlabeled
  - batch_size
  seed_param: seed
  paradigm_extras_forwarding: by_name_from_config
  forbidden_param_names:
  - config
  - batch_output
  - batch_outputs
rationale: "The pluggable function is called uniformly via\n`select_batch(model, x_unlabeled,\
  \ batch_size, **kwargs)` \u2014 by the v1\nharness in archived runs, and by the\
  \ v2 notebook's \xA75 acquisition loop\nin v2 runs. Both forms inspect the function's\
  \ signature and look each\nnamed param up in the surrounding config. A `config:\
  \ dict` parameter\ncannot be filled this way; methods that need extra params declare\n\
  them as named keyword parameters with defaults (e.g.,\n`mc_samples: int = 100`,\
  \ `core_set_size: int = 100`). `batch_returns`\nmay be used as a larger candidate\
  \ prefilter, but the selector's\nreturned batch count is always the fixed `batch_size`\
  \ argument;\n`batch_output`/`batch_outputs` are forbidden because they split\nprovenance\
  \ from the actual acquisition count. The validator rejects\nany pluggable function\
  \ whose signature contains a forbidden param name.\n\n`select_batch` is a pure acquisition\
  \ function from the notebook/harness\npoint of view: it returns positions into the\
  \ exact `x_unlabeled` tensor it\nwas passed. It must not silently add unlabeled\
  \ examples to `x_labeled`,\nremove examples from `x_unlabeled`, or maintain hidden\
  \ acquisition state\nunless every affected unlabeled position is included in the\
  \ returned list.\nMethod-specific initialization steps (for example, a core-set\
  \ bootstrap)\nbelong in a separate top-level helper that the notebook calls once\
  \ during\n\xA73.4 and records in `labeled_idx`/`unlabeled_idx`.\n"
```

## smoke_economics

```yaml
max_budget_to_pool_ratio: 0.05
floors:
  batch_size:
    absolute_min: 50
    reason: "Batch-acquisition diversity is not meaningfully tested below B\u2248\
      50."
```

## paradigm_extras

```yaml
core_set_size:
  value: 100
  reasoning_template: "Paper uses a {paper_value}-point core-set. We use {value} \u2014\
    \ smaller core-set runs Stage 1 fast and still seeds the per-round acquisition\
    \ with a diverse initial labeled set."
batch_returns:
  value: 200
  reasoning_template: "Paper sets b at ~3 \xD7 b' (the batch_size). We use 2 \xD7\
    \ 100 = {value} \u2014 enough to give the geometric ranking room without spending\
    \ compute on candidates the ranker will reject."
```

## model_defaults

```yaml
hidden_dim:
  value: 256
  reasoning_template: Paper states no data-type-keyed MLP width. We use hidden_dim={value}
    as the taxonomy-typical AL convention.
```
