<!-- Generated by scripts/refresh_ssot_docs.py taxonomy role views. -->
# active_learning/batch_acquisition - reviewer view

Source taxonomy node: `TE-TS/active_learning/batch_acquisition`
Legacy paradigm id: `active_learning/batch_acquisition`
Inheritance chain: active_learning -> active_learning/batch_acquisition

This generated view is maintainer documentation rendered from `docs/ssot/taxonomies.yaml`; runtime consumers should use `scripts.taxonomy` and `scripts.build_plan`.

## semantic_checks

```yaml
- id: core_method_summary_matches_paper
  stage: stage_1_analyzer
  check: 'core_method.summary names the actual acquisition mechanism (e.g. "diverse,
    uncertain gradient embeddings + k-MEANS++"), not a generic "improves active learning".

    '
  silent_failure: A vague summary passes schema validation but misrepresents the contribution.
  severity: warning
  status: seed
- id: critical_requirements_essential_features
  stage: stage_1_analyzer
  check: "Only architecture-load-bearing features (MC dropout active at inference,\
    \ penultimate-layer hook) are tagged severity: essential \u2014 not everything\
    \ the method merely uses.\n"
  silent_failure: Over-tagging essential features forces unnecessary architecture
    constraints downstream.
  severity: warning
  status: seed
- id: AL-analyzer-scale-hyperparams
  stage: stage_1_analyzer
  check: 'Every distance threshold / similarity radius / kernel bandwidth the paper
    uses appears in critical_requirements.scale_dependent_hyperparameters with paper_value/formula,
    exactly one schema-1.13 typed calibration_context, and a description. Feature
    magnitude and target-box-grid conventions use their distinct arms; unsupported
    physical or mixed contexts use other. Temporal counts, scale-free values, and
    graph statistics do not enter this lane.

    '
  silent_failure: A missing scale-dependent hyperparameter causes silent algorithm
    drift when preprocessing differs from the paper's scale.
  severity: error
  status: seed
- id: essential_features_implemented_not_just_annotated
  stage: stage_2b_architecture
  check: 'Each "# essential: <name>" annotation in model.py is actually implemented,
    not just labelled (dropout layers present AND kept active; forward_with_embedding
    returns the real penultimate activation, not a copy of the logits).

    '
  silent_failure: An annotated-but-unimplemented feature passes structural validators
    while the method loses its mechanism.
  severity: error
  status: seed
- id: architecture_substitution_documented
  stage: stage_2b_architecture
  check: 'Any architecture substitution (MLP for the paper''s ResNet, reduced depth)
    is named in the module docstring with the silent-failure mode if a replacement
    loses the substituted feature.

    '
  silent_failure: Undocumented substitutions mislead users about paper fidelity.
  severity: warning
  status: seed
- id: training_protocol_matches_paper
  stage: stage_2b_architecture
  check: 'training.py''s optimizer, learning rate, retrain-from-scratch protocol,
    and stopping criterion match spec.critical_requirements.training.

    '
  silent_failure: "Paper uses Adam + retrain-from-scratch but training.py uses SGD\
    \ or warm-starts \u2014 a regime change disguised as a faithful port."
  severity: warning
  status: seed
- id: paper_element_implementations_match_paper
  stage: stage_2c_method
  check: 'For each "# paper-element: <id>" annotation in method.py, the code at that
    site implements what the referenced paper_map element (label + section) describes.

    '
  silent_failure: Docstring claims Eq. 13 on penultimate features but the code uses
    raw input features.
  severity: error
  status: seed
- id: paper_scale_faithful_implementation
  stage: stage_2c_method
  check: "Demo-scale and paper-scale runs are the SAME code differing only in parameter\
    \ values. No behaviour-changing simplification (dropped regularization term, heuristic\
    \ substitution, hard-coded inner-loop count) \u2014 those must be implemented,\
    \ not documented.\n"
  silent_failure: 'A behaviour-changing simplification is hidden behind a # paper-fidelity
    comment instead of being removed.'
  severity: error
  status: seed
- id: simplifications_flagged
  stage: stage_2c_method
  check: 'Every smoke-scale accommodation that is a genuine no-op at paper scale (clamps,
    full-batch fallbacks) carries a "# paper-fidelity:" note or docstring mention.

    '
  silent_failure: Undocumented accommodations leave the reader unable to trust which
    deviations are intentional.
  severity: warning
  status: seed
- id: pluggable_function_actually_does_the_method
  stage: stage_2c_method
  check: 'The pluggable component (spec.comparison.pluggable_component.name) implements
    the paper''s algorithm, not a degenerate fallback (e.g. pure top-k uncertainty
    when the paper specifies uncertainty + diversity).

    '
  silent_failure: The selector falls back to a baseline and the docstring is edited
    to match the degenerate behaviour.
  severity: error
  status: observed
  probe: claims.contribution_floor
- id: AL-method-pluggable-no-hidden-state
  stage: stage_2c_method
  check: "select_batch returns positions into the exact x_unlabeled tensor it was\
    \ passed; it does not mutate x_labeled/x_unlabeled or hold hidden acquisition\
    \ state unless every affected position is in the returned list. One-time init\
    \ (e.g. GBALD core-set) is a separate \xA73.4 helper.\n"
  silent_failure: Hidden extra acquisitions inside select_batch make the learning-curve
    budget and the method's scoring state disagree.
  severity: error
  status: observed
  probe: al_loop.acquisition_contract
- id: AL-stage1-coreset-behaves
  stage: stage_2c_method
  check: "The one-time Stage-1 core-set constructor (the \xA73.4 init helper, found\
    \ by the core_set/coreset naming convention) behaves like a selection: unique\
    \ in-range indices, picks that carry data signal on a cluster-sorted synthetic\
    \ pool (a constant-score argmax concentrates in the leading block), and an eta\
    \ channel that actually moves the selection when the paper's mechanism is an ellipsoid\
    \ rescale. No convention match means unprobeable, stated \u2014 never a fabricated\
    \ stand-in.\n"
  silent_failure: The demo executes a core-set whose selection criterion is dead (constant
    scores, position-order picks), so the paper's Stage-1 contribution never runs
    and nothing notices.
  severity: error
  status: observed
  probe: al_stage1.coreset_construction
- id: AL-method-ranking-monotonicity
  stage: stage_2c_method
  check: 'Any comment/docstring explaining a ranking score matches the score''s monotonic
    direction (an inverse-distance score sorted descending gives closer points higher
    scores).

    '
  silent_failure: Prose says 'farther samples score higher' while the formula does
    the opposite.
  severity: warning
  status: seed
- id: paper_source_values_match_paper
  stage: stage_2x_params
  check: 'Every param with source: paper is actually stated in the paper; values pulled
    from related-work conventions are source: system_inferred.

    '
  silent_failure: 'An inferred value mislabelled source: paper overstates fidelity.'
  severity: warning
  status: seed
- id: system_default_reasoning_explains_deviation
  stage: stage_2x_params
  check: 'Every source: system_default param''s reasoning explains the deviation from
    paper_value (smoke runtime, hardware, demo-vs-benchmark).

    '
  silent_failure: Missing or taxonomy-contradicting reasoning hides an unjustified
    default.
  severity: warning
  status: seed
- id: AL-params-demo-reachability
  stage: stage_2x_params
  check: 'The delivered demo config exercises the acquisition loop: initial_labeled
    < pool_size and the per-round budget does not exhaust the pool before num_rounds
    complete.

    '
  silent_failure: initial_labeled >= pool_size leaves the pool empty at round 0; every
    select_batch returns [] and the smoke gate passes a demo whose contribution never
    runs.
  severity: error
  status: observed
  probe: al_loop.demo_config_reachability
- id: AL-nb-above-chance
  stage: stage_3a_notebook
  check: Final accuracy exceeds chance (1 / num_classes) on the executed notebook.
  silent_failure: A miswired loop reports high accuracy by evaluating against training
    labels or a constant predictor.
  severity: warning
  status: seed
- id: notebook_narrative_matches_code
  stage: stage_3a_notebook
  check: "Each \xA74 subsection's markdown matches the code below it (cites the equation\
    \ the code computes; describes the path the code takes).\n"
  silent_failure: Markdown describes full behaviour but the code calls one path; cites
    Eq. X but computes Eq. Y.
  severity: warning
  status: seed
- id: paper_citations_accurate
  stage: stage_3a_notebook
  check: "\xA74 bold paper-section/equation citations point to the right location\
    \ in the paper."
  silent_failure: Misattributed citations send readers to the wrong section.
  severity: warning
  status: seed
- id: AL-nb-loop-evaluates-each-round
  stage: stage_3a_notebook
  check: "\xA75.1's loop trains AND evaluates each round; the learning curve reflects\
    \ the retrained model, not a placeholder echo of the warmup accuracy.\n"
  silent_failure: test_accuracies.append(test_accuracies[-1]) placeholder leaves a
    meaningless curve that still 'rises'.
  severity: error
  status: seed
  probe: al_loop.eval_label_alignment
- id: AL-nb-loop-retrains-from-scratch
  stage: stage_3a_notebook
  check: "\xA75.1 builds a FRESH model (build_model inside the round loop) before\
    \ training, unless the paper deliberately warm-starts.\n"
  silent_failure: Reusing one model object across rounds is warm-starting; silent
    (no crash, curve still rises) and changes the algorithmic regime.
  severity: error
  status: observed
  probe: al_loop.fresh_retrain
- id: AL-nb-loop-index-space
  stage: stage_3a_notebook
  check: "\xA75.1 maps select_batch's returned positions back to global indices through\
    \ a maintained unlabeled-index array (chosen = unlabeled_idx[positions]); no positional-offset\
    \ arithmetic; labeled/unlabeled stay disjoint.\n"
  silent_failure: "Offset arithmetic assumes the labeled set is a contiguous pool\
    \ prefix; silent \u2014 the curve still rises because more labels help."
  severity: error
  status: observed
  probe: al_loop.microharness
- id: AL-nb-loop-acquisition-round-semantics
  stage: stage_3a_notebook
  check: 'num_rounds means acquisition rounds, not plotted points: evaluate the initial
    set once, then acquire-and-evaluate once per round (curve has num_rounds+1 points).
    No "r < num_rounds-1" guard; no unevaluated final batch.

    '
  silent_failure: Off-by-one round shapes leave an acquired-but-unevaluated batch
    or a missing point.
  severity: error
  status: seed
- id: AL-nb-ranking-monotonicity
  stage: stage_3a_notebook
  check: "Notebook markdown explaining ranking scores matches the code's monotonic\
    \ direction (inverse-distance ranked descending \u2192 closer points score higher).\n"
  silent_failure: Prose contradicts the sort direction.
  severity: warning
  status: seed
- id: AL-batch-forward-embedding
  stage: stage_2b_architecture
  check: "The architecture exposes forward_with_embedding(x) returning the real penultimate\
    \ activation \u2014 BADGE-shaped gradient-embedding methods require it.\n"
  silent_failure: A stub returning logits-as-embedding makes the diversity selection
    degenerate to pure uncertainty.
  severity: error
  status: seed
- id: AL-batch-size-preserved
  stage: stage_2x_params
  check: 'batch_size matches the paper''s claimed batch regime; it is not reduced
    to fit a time budget (that changes the algorithm, not just the scale).

    '
  silent_failure: A demo at B=15 for a B=1000 paper tests a different, smaller-batch
    algorithm than the paper's claim.
  severity: warning
  status: seed
- id: AL-kmeanspp-incremental-distances
  stage: stage_2c_method
  check: 'k-MEANS++/k-means++ helpers maintain a running nearest-distance vector instead
    of materializing an (N, k, D) all-centers broadcast.

    '
  silent_failure: Toy cells pass, but smoke-scale batch acquisition can allocate tens
    of GB and time out before the notebook proves the method.
  severity: error
  status: seed
```

## pluggable_component

```yaml
name: select_batch
return_type: List[int]
signature_template: select_batch(model, x_unlabeled, batch_size, seed, *paradigm_extras_by_name)
  -> List[int]
contract:
  fixed_positional_args:
  - model
  - x_unlabeled
  - batch_size
  seed_param: seed
  paradigm_extras_forwarding: by_name_from_config
  forbidden_param_names:
  - config
  - batch_output
  - batch_outputs
rationale: "The pluggable function is called uniformly via\n`select_batch(model, x_unlabeled,\
  \ batch_size, **kwargs)` \u2014 by the v1\nharness in archived runs, and by the\
  \ v2 notebook's \xA75 acquisition loop\nin v2 runs. Both forms inspect the function's\
  \ signature and look each\nnamed param up in the surrounding config. A `config:\
  \ dict` parameter\ncannot be filled this way; methods that need extra params declare\n\
  them as named keyword parameters with defaults (e.g.,\n`mc_samples: int = 100`,\
  \ `core_set_size: int = 100`). `batch_returns`\nmay be used as a larger candidate\
  \ prefilter, but the selector's\nreturned batch count is always the fixed `batch_size`\
  \ argument;\n`batch_output`/`batch_outputs` are forbidden because they split\nprovenance\
  \ from the actual acquisition count. The validator rejects\nany pluggable function\
  \ whose signature contains a forbidden param name.\n\n`select_batch` is a pure acquisition\
  \ function from the notebook/harness\npoint of view: it returns positions into the\
  \ exact `x_unlabeled` tensor it\nwas passed. It must not silently add unlabeled\
  \ examples to `x_labeled`,\nremove examples from `x_unlabeled`, or maintain hidden\
  \ acquisition state\nunless every affected unlabeled position is included in the\
  \ returned list.\nMethod-specific initialization steps (for example, a core-set\
  \ bootstrap)\nbelong in a separate top-level helper that the notebook calls once\
  \ during\n\xA73.4 and records in `labeled_idx`/`unlabeled_idx`.\n"
```

## build_plan

```yaml
schema_version: '1.0'
source: spec+taxonomy
interface_hint: select_batch(model, x_unlabeled, batch_size, seed, *paradigm_extras_by_name)
  -> List[int]
```

## package_manifest

```yaml
description: Spec-derived structural contract for method/ files. Producer ownership
  is fixed by the pipeline; pluggable names/signatures and additional model methods
  come from method_spec.json.
files:
- path: method/data.py
  kind: paradigm_fixed
  produced_by: package_scaffolder
  public_symbols:
  - name: load_data
    kind: function
    signature: 'load_data(path: str | os.PathLike | None = None, *, pool_size: int
      = 5000, n_test: int = 1000, seed: int = 0) -> Tuple[torch.Tensor, torch.Tensor,
      torch.Tensor, torch.Tensor]'
- path: method/example_data/README.md
  kind: paradigm_fixed
  produced_by: package_scaffolder
- path: method/model.py
  kind: paradigm_shaped
  produced_by: architecture_coder
  public_symbols:
  - name: <ArchitectureClass>
    kind: class
    inherits: torch.nn.Module
    required_methods:
    - 'forward(self, x: torch.Tensor) -> torch.Tensor'
- path: method/training.py
  kind: paradigm_fixed
  produced_by: architecture_coder
  public_symbols:
  - name: build_model
    kind: function
    signature: 'build_model(input_dim: int, n_classes: int, **arch_kwargs) -> <ArchitectureClass>'
  - name: train_from_scratch
    kind: function
    signature: 'train_from_scratch(model: <ArchitectureClass>, x_train: torch.Tensor,
      y_train: torch.Tensor, *, learning_rate: float, max_epochs: int, train_until_accuracy:
      float, seed: int) -> <ArchitectureClass>'
- path: method/method.py
  kind: method_shaped
  produced_by: method_coder
  public_symbols:
  - name: select_batch
    kind: function
    signature: select_batch(model, x_unlabeled, batch_size, seed, *paradigm_extras_by_name)
      -> List[int]
    contract:
      fixed_positional_args:
      - model
      - x_unlabeled
      - batch_size
      seed_param: seed
      paradigm_extras_forwarding: by_name_from_config
      forbidden_param_names:
      - config
      - batch_output
      - batch_outputs
  - name: <method_helpers>
    kind: function
    all_top_level_functions_are_public: true
- path: method/__init__.py
  kind: derived
  produced_by: init_finalizer
  content_rule: 'Re-export every public symbol declared above. __all__ ordering: [pluggable_component.name,
    *method_helpers, *architecture_classes, *training_functions, load_data].'
- path: requirements.txt
  kind: derived
  produced_by: init_finalizer
  content_rule: Computed by AST-walking imports across method/ and notebook imports;
    base requirements are owned by finalize_package_init.py.
- path: method/README.md
  kind: paradigm_fixed
  produced_by: package_scaffolder
dependency_graph:
  method/data.py: []
  method/model.py: []
  method/method.py:
  - method/model.py
  method/training.py:
  - method/model.py
  method/__init__.py:
  - method/data.py
  - method/method.py
  - method/model.py
  - method/training.py
validators:
  after_package_scaffolder: scripts/validate_scaffolder_output.py
  after_architecture_coder: scripts/validate_architecture_coder_output.py
  after_method_coder: scripts/validate_method_coder_output.py
  after_init_finalizer:
  - scripts/validate_package_imports.py
  - scripts/validate_arch_contract.py
  - scripts/validate_arch_contract_runtime.py
```

## arch_contract_requirements

```yaml
description: Spec-derived active-learning architecture contract requirements. Concrete
  shapes remain paper/code-derived; this only validates the universal AL contract
  surface.
required_blocks:
  data_loader.load_data_returns:
    type: dict[str, shape_string]
    min_keys:
    - x_pool
    - y_pool
    - x_test
    - y_test
    exact_keys:
    - x_pool
    - y_pool
    - x_test
    - y_test
  architecture.model:
    required_subblocks:
    - class_name
    - forward.input
    - forward.output_type
  pluggable_component.name:
    must_equal: spec.comparison.pluggable_component.name
  pluggable_component.input_shapes:
    type: dict[str, shape_string]
    min_keys:
    - x_unlabeled
  pluggable_component.output_shape:
    type: shape_string
  training_loop.function_name:
    must_equal: train_from_scratch
  training_loop.input_shapes:
    type: dict[str, shape_string]
    min_keys:
    - x_train
    - y_train
symbol_conventions:
  B: runtime batch size inside model forward
  N: training-set size
  N_test: test-set size
  N_pool: unlabeled pool size
  n_features: input feature dim
  n_classes: number of classes
  hidden_dim: model hidden width when applicable
```

## notebook_layout

```yaml
description: 'Section structure for active-learning notebooks. Each section is a

  declarative description of what the notebook-generator emits at that

  position; the actual cells are written by the agent based on

  method_spec.json + paper_map.json + the generated method/ package.

  '
sections:
- id: title
  title: Paper title (from spec.paper.title)
  kind: agnostic
  content: 'Markdown-only. Top-level # heading with the paper''s full citation

    (authors, year, venue), a short ''what this notebook gives you'' bullet

    list, a ''two ways to use this'' bullet list (run-as-is vs. import from

    own code), and a ''what this notebook does NOT do'' note (this is a

    reference implementation, not a benchmark reproduction; defer to the

    paper for empirical claims).

    '
- id: install
  title: '## 0. Install dependencies (first run only)'
  kind: agnostic
  content: "One markdown cell explaining when to run, one code cell running\n`%pip\
    \ install -r requirements.txt` (NOT `!pip` \u2014 the magic is needed\non systems\
    \ where `pip` is not in the shell PATH). Cell can sit live;\nrunning on an already-installed\
    \ system is a no-op.\n"
- id: setup
  title: '## 1. Setup'
  kind: agnostic
  content: "One code cell: `%matplotlib inline`, imports (matplotlib.pyplot, numpy,\n\
    torch, then `from method import (...)` listing the public API the\nnotebook calls),\
    \ master `SEED = 42`, seed numpy + torch globals.\nThe import list is determined\
    \ from method/__init__.py's __all__;\nany helper used in \xA74 must be exported\
    \ there.\n"
- id: parameters
  title: '## 2. Parameters'
  kind: agnostic
  content: "Markdown header explaining the provenance schema (paper / system_default\n\
    / system_inferred), then the **rendered provenance table FIRST**, then\nthe params\
    \ dict code cell, then an \"Optional: scale up to paper-faithful\nvalues\" subsection\
    \ with a commented-out cfg.update() block.\nTable-first ordering: readers scan\
    \ provenance at a glance; code is\nbelow for editing. Table is generated once\
    \ at notebook-build time from\nthe params dict and embedded as static markdown\
    \ \u2014 does not auto-update\nif the user edits params downstream.\n"
- id: setup_pieces
  title: '## 3. The setup pieces'
  kind: paradigm
  content: "Brief overview markdown that names the substrate (model + training +\n\
    data + bootstrap) and explicitly tells the reader: \"these are paper\nprotocol\
    \ but NOT the paper's contribution; skim and move on to \xA74\".\nMention any\
    \ paradigm-specific architecture requirements (for AL/bayesian:\ndropout active\
    \ at inference; for AL/batch_acquisition: forward_with_embedding\ncontract). Pull\
    \ these from the matched paradigm's `pluggable_component`\nand `architecture`\
    \ blocks.\n"
  subsections:
  - id: data
    title: '### 3.1 Data'
    content: "One paragraph explaining `load_data()` defaults to\nmethod/example_data/,\
      \ downloads MNIST on first run, points to \xA76\nfor custom data. One code cell\
      \ calling load_data with cfg-derived\nargs, printing pool/test/classes shapes.\n"
  - id: model
    title: '### 3.2 Model'
    content: 'One paragraph naming the paper''s architecture choice and what

      we ship vs. swap-in options. Highlight the paradigm-specific

      constraint (e.g., ''forward_with_embedding hook'' for batch_acquisition,

      ''dropout active at inference'' for bayesian). One code cell:

      `model = build_model(...); print(model)` to show the architecture.

      '
  - id: training
    title: '### 3.3 Training'
    content: "One paragraph naming the protocol (Adam, retrain-from-scratch,\ntrain-until-99%,\
      \ paper section reference). NO code cell \u2014 training\nhappens per-round\
      \ in \xA73.4 (bootstrap) and \xA75.1 (the loop). State\nthis explicitly so the\
      \ reader doesn't expect a cell.\n"
  - id: bootstrap
    title: '### 3.4 Bootstrap labeled set'
    content: "For AL: pick `initial_labeled` random examples (or run a method-\nspecific\
      \ bootstrap routine if the spec declares one \u2014 e.g.,\nGBALD's `construct_core_set`),\
      \ train a warmup model on them.\nThe warmup model is what \xA74's per-component\
      \ demos use. If the\nmethod has a Stage-1-style initialization (like GBALD),\
      \ call it\nhere and reference \xA74.x for the algorithmic explanation.\n"
- id: method
  title: "## 4. The {method_name} method \u2B50"
  kind: method_specific
  content: "The paper's contribution, broken into the method's own components.\nFirst\
    \ subsection is always 'Intuition' \u2014 a markdown cell motivating\nWHY the\
    \ method's design choice solves the problem. Subsequent\nsubsections come from\
    \ the spec's method components (paper-element IDs\nin paper_map.json grouped by\
    \ algorithmic function). Each component-\nsubsection has: (a) markdown explaining\
    \ the math with paper section/\nequation references in **bold**, (b) a code cell\
    \ exercising that piece\non the warmup model from \xA73.4 (small visualization\
    \ or printed output\nshowing the piece does what it says). Final subsection is\
    \ 'Putting it\ntogether' \u2014 markdown + small code block showing how the wrapper\n\
    composes the components. Mark novel pieces with \u2B50 (the paper's actual\ncontribution;\
    \ method components that are off-the-shelf \u2014 e.g., k-MEANS++\nin BADGE \u2014\
    \ get cited but no \u2B50).\n"
  generator_instructions: "Walk paper_map.json's element list. Group elements by their\
    \ algorithmic\nfunction: motivation/intuition (concept-* IDs), distinct novel\
    \ pieces\n(each with its own subsection \u2014 typically eq-* or alg-* IDs), and\
    \ the\nwrapper. For each piece, the markdown cell cites the paper element's\n\
    `section` field. The exercise code cell uses helpers from\n`method.method` (compute_gradient_embeddings,\
    \ kmeans_plus_plus_seeding,\netc.) \u2014 these must be in method/__init__.py\
    \ or accessible via\nmethod.method to be imported in \xA71.\n"
- id: running
  title: '## 5. Running active learning end-to-end'
  kind: paradigm
  content: "One paragraph naming the experimental protocol from the paper's\nSection\
    \ 4 (Experiments). State that `labeled` and `unlabeled` continue\nfrom \xA73.4's\
    \ bootstrap state.\n"
  subsections:
  - id: loop
    title: '### 5.1 The acquisition loop'
    content: "Define `evaluate(model, x, y)` (eval mode, no_grad, argmax accuracy),\n\
      then run the AL loop. Two things here are correctness-critical and\nhave silently\
      \ broken past runs (they don't crash and the learning\ncurve still rises, so\
      \ they pass smoke) \u2014 follow them exactly:\n\n(1) RETRAIN FROM SCRATCH each\
      \ round. Build a FRESH model with\n    `build_model(...)` at the TOP of every\
      \ round, then\n    `train_from_scratch` it on the current labeled set. Do NOT\
      \ carry\n    one model object across rounds (and do NOT reuse \xA73.4's warmup\n\
      \    model) \u2014 that is warm-starting, which BADGE and most modern AL\n \
      \   explicitly avoid because it changes the algorithmic regime. (Only\n    if\
      \ the paper deliberately warm-starts should you reuse the model \u2014\n   \
      \ follow `spec.critical_requirements.training` and say so.)\n\n(2) INDEX BOOKKEEPING.\
      \ `select_batch` returns positions INTO the\n    tensor you passed it (the current\
      \ unlabeled pool) \u2014 NOT global\n    pool indices. Keep an explicit array\
      \ of the unlabeled examples'\n    global indices and map back through it. NEVER\
      \ recover globals with\n    positional-offset arithmetic (e.g. `i - len(initial_indices)`),\n\
      \    which falsely assumes the labeled set is a contiguous prefix of\n    the\
      \ pool. Keep `labeled`/`unlabeled` as DISJOINT index sets (set\n    difference\
      \ on update) so an already-labeled example can never be\n    re-selected.\n\n\
      (3) ACQUISITION-ROUND SEMANTICS. `cfg[\"num_rounds\"]` means the\n    number\
      \ of acquisition rounds, not the number of plotted\n    evaluation points. Evaluate\
      \ the initial labeled set once before\n    any acquisition, then acquire exactly\
      \ once per round and append\n    the post-acquisition evaluation. The resulting\
      \ learning curve\n    has `cfg[\"num_rounds\"] + 1` points. Do NOT guard the\
      \ final\n    `select_batch` call with `r < cfg[\"num_rounds\"] - 1`, and do\n\
      \    NOT acquire a final batch that is never evaluated.\n    Do NOT evaluate\
      \ before the label merge and then append that\n    accuracy with the post-merge\
      \ `len(labeled_idx)`; the label\n    count and accuracy must describe the same\
      \ trained model state.\n\n(4) VISIBLE ACQUISITION STATE. The notebook owns the\
      \ active-learning\n    state. If a method needs an initialization/core-set/bootstrap\n\
      \    acquisition step, call the helper once in \xA73.4 and record those\n  \
      \  returned indices in `labeled_idx`. The per-round\n    `select_batch(...)`\
      \ call must return every new unlabeled\n    position that should become labeled\
      \ that round. Do NOT rely on\n    `select_batch` to internally add extra unlabeled\
      \ examples to a\n    temporary labeled set and then return only a later subset;\
      \ that\n    makes the learning curve's labeled counts and the method's\n   \
      \ actual scoring state disagree.\n\nReference skeleton \u2014 keep this structure;\
      \ adapt names to the spec:\n\n    import numpy as np\n    labeled_idx = np.asarray(initial_indices)\n\
      \    unlabeled_idx = np.setdiff1d(np.arange(len(x_pool)), labeled_idx)\n   \
      \ learning_curve = []\n\n    def train_eval_current(round_seed):\n        model\
      \ = build_model(...)                    # FRESH \u2192 retrain from scratch\n\
      \        model = train_from_scratch(\n            model, x_pool[labeled_idx],\
      \ y_pool[labeled_idx],\n            ..., seed=round_seed)\n        acc = evaluate(model,\
      \ x_test, y_test)\n        learning_curve.append((len(labeled_idx), acc))\n\
      \        return model\n\n    model = train_eval_current(SEED)              \
      \   # initial labeled set\n    for r in range(cfg[\"num_rounds\"]):\n      \
      \  pos = select_batch(\n            model, x_pool[unlabeled_idx], cfg[\"batch_size\"\
      ], SEED + r)\n        chosen = unlabeled_idx[np.asarray(pos)]      # positions\
      \ \u2192 GLOBAL indices\n        labeled_idx = np.union1d(labeled_idx, chosen)\n\
      \        unlabeled_idx = np.setdiff1d(unlabeled_idx, chosen)\n        model\
      \ = train_eval_current(SEED + r + 1)     # post-acquisition eval\n\n\xA75.1\
      \ trains and evaluates the initial labeled set, then trains and\nevaluates after\
      \ each acquisition round. The curve must reflect the\nretrained model, not the\
      \ warmup. Print per-round status.\n"
  - id: learning_curve
    title: '### 5.2 Learning curve'
    content: 'Plot learning_curve as a single line chart (labels acquired vs.

      test accuracy). Title names the method + dataset + smoke-sized.

      '
- id: own_data
  title: '## 6. Use your own data'
  kind: agnostic
  content: 'Two options (Option A: pass an explicit path; Option B: drop file in

    method/example_data/ to override default), three formats (.pt / .json

    / .csv) with examples of each, architecture-swap note pointing at

    method/model.py for image-shaped tensors.

    '
```
