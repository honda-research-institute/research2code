<!-- Generated by scripts/refresh_ssot_docs.py taxonomy role views. -->
# active_learning/bayesian - analyzer view

Source taxonomy node: `TE-TS/active_learning/bayesian`
Legacy paradigm id: `active_learning/bayesian`
Inheritance chain: active_learning -> active_learning/bayesian

This generated view is maintainer documentation rendered from `docs/ssot/taxonomies.yaml`; runtime consumers should use `scripts.taxonomy` and `scripts.build_plan`.

## fingerprint

```yaml
what_it_is: 'Estimates epistemic uncertainty via posterior approximation (MC dropout
  / ensembles / SVI) and selects for expected information gain (BALD-family); scoring
  is multi-pass with dropout active at inference.

  '
positive_signals:
- mutual information / BALD acquisition over posterior samples
- T stochastic forward passes per example (MC dropout)
route_elsewhere:
- signal: selection is fixed/random, no model-in-the-loop
  to: TE-TS/semi_supervised_learning
- signal: the contribution is the loss/objective, not the selection rule
  to: TE-LF/task_specific_loss
worked_examples:
- BALD (Houlsby et al. 2011; Gal et al. 2017)
- BatchBALD (Kirsch et al. 2019)
- GBALD (Cao & Tsang 2022)
```

## priors

```yaml
architecture:
  requires: dropout_layers_active_at_inference
  typical_dropout_rate:
  - 0.25
  - 0.5
seeds:
  minimum_for_ranking_claim: 3
  recommended_for_publication: 5
calibration:
  preserve:
  - mc_samples
  demo_minimum_mc_samples: 20
benchmark_selection: 'Prefer the smallest standard benchmark for data_setup (MNIST
  < OpenML < SVHN < CIFAR-10 < ...); all four numeric fields come from the same benchmark
  configuration. See content file.

  '
posterior_approximation:
  default: mc_dropout
  alternatives:
  - ensembles
  - svi
  - laplace_approximation
  mc_samples_typical_range:
  - 100
  - 2000
  mc_samples_minimum_for_meaningful_test: 50
```

## scaffold_hints

```yaml
interface_hint: select_batch(model, x_unlabeled, x_labeled, batch_size, seed, *paradigm_extras_by_name)
  -> List[int]
content_file: docs/ssot/content/active_learning.md
templates_dir: paradigms/supervised_ml/active_learning/templates
demo_data_source: template_downloads
```

## pluggable_component

```yaml
name: select_batch
return_type: List[int]
signature_template: select_batch(model, x_unlabeled, x_labeled, batch_size, seed,
  *paradigm_extras_by_name) -> List[int]
contract:
  fixed_positional_args:
  - model
  - x_unlabeled
  - x_labeled
  - batch_size
  seed_param: seed
  paradigm_extras_forwarding: by_name_from_config
  forbidden_param_names:
  - config
  - batch_output
  - batch_outputs
rationale: "Bayesian-AL methods are multi-step per acquisition round: they score\n\
  uncertainty (via MC dropout / posterior samples) but also reason about\ndiversity\
  \ or representativeness with respect to the already-labeled set\n(e.g., GBALD's\
  \ Eq. 13 computes distances to labeled-set penultimate\nfeatures). The pluggable\
  \ function therefore needs read access to\n`x_labeled` so it can implement what\
  \ the paper describes. Methods that\nonly need uncertainty (e.g., vanilla BALD)\
  \ accept the parameter and\nignore it; the cost of a one-extra-positional-arg is\
  \ small compared to\nthe cost of silently degrading multi-step methods to pure top-k.\n\
  \nStateless beyond the signature: the function must NOT mutate `x_labeled`\nor store\
  \ it \u2014 it's read-only state passed in by the notebook each round.\n"
```

## smoke_economics

```yaml
max_budget_to_pool_ratio: 0.4
floors:
  mc_samples:
    absolute_min: 20
    reason: BALD's posterior estimate collapses toward a point estimate below about
      20 MC samples.
```

## paradigm_extras

```yaml
mc_samples:
  value: 20
  reasoning_template: "Paper uses {paper_value} MC dropout samples. We use {value}\
    \ \u2014 the bayesian-AL taxonomy minimum for BALD's posterior estimate to be\
    \ meaningful (Gal et al. recommend T \u2265 20). Larger T at paper scale gives\
    \ a tighter estimate but dominates runtime at smoke scale."
```

## model_defaults

```yaml
hidden_dim:
  value: 256
  reasoning_template: Paper states no data-type-keyed MLP width. We use hidden_dim={value}
    as the taxonomy-typical AL convention.
dropout_rate:
  value: 0.5
  reasoning_template: Bayesian-AL taxonomy recommends dropout in [0.25, 0.5] (Gal
    & Ghahramani 2016); {value} is the taxonomy-typical default. Paper does not specify
    a dropout rate.
```
