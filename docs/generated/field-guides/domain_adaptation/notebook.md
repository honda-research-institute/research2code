<!-- Generated by scripts/refresh_ssot_docs.py taxonomy role views. -->
# domain_adaptation - notebook view

Source taxonomy node: `DA-UDA/domain_adaptation`
Legacy paradigm id: `domain_adaptation`
Inheritance chain: domain_adaptation

This generated view is maintainer documentation rendered from `docs/ssot/taxonomies.yaml`; runtime consumers should use `scripts.taxonomy` and `scripts.build_plan`.

## pluggable_component

```yaml
name: generate_pseudo_labels
return_type: PseudoLabelSet
signature_template: generate_pseudo_labels(detectors, target_data, seed, *paradigm_extras_by_name)
  -> PseudoLabelSet
contract:
  fixed_positional_args:
  - detectors
  - target_data
  seed_param: seed
  paradigm_extras_forwarding: by_name_from_config
  forbidden_param_names:
  - config
return_type_definition: 'PseudoLabelSet is a structured object containing boxes, scores,
  class ids, and statistics per target-domain frame.

  '
rationale: 'The method-owned pluggable is the pseudo-label generation pipeline; training.py
  owns retraining on those pseudo-labels.

  '
```
