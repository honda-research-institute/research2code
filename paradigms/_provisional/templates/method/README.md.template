# {{ paper_title }}

Reference implementation of {{ method_name }}, generated by R2C from the
following paper:

> {{ paper_authors }}. *{{ paper_title }}*.

{{ method_summary }}

## About this package

This paper's method comes from a research area that is new to the R2C
paradigm library, so the package was scaffolded from the neutral starting
kit rather than a field-specific one. The method itself: {{ method_statement }}

The package's central entry point, as declared by the analysis of the paper:

```
{{ interface_signature }}
```

## What's in this directory

```
{{ run_slug }}/
├── notebook.ipynb         # walks through using the package end-to-end
├── method/                # importable Python package
│   ├── __init__.py        # public API
│   ├── method.py          # the paper's contribution (the algorithm)
│   ├── model.py           # supporting architecture / components
│   ├── training.py        # training / execution procedure
│   ├── data.py            # data loading (local files; nothing is downloaded)
│   └── example_data/      # drop your own dataset files here
├── requirements.txt
└── README.md
```

## Quick start

```bash
pip install -r requirements.txt
jupyter notebook notebook.ipynb
```

Click **Run All**. The notebook drives `{{ interface_name }}` on a small
demonstration input to verify the implementation is correct and shows you
how to drive it.

## Use the package from your own code

See `notebook.ipynb` for a complete walk-through. The public API is documented
in `method/__init__.py`.

## Notes on faithfulness

This is a reference implementation, not a benchmark reproduction. The paper's
empirical comparisons are not reproduced here — the notebook runs the method
alone at demonstration scale to verify the implementation is correct.

The notebook's `params` dict makes provenance explicit: each parameter records
whether its value comes from the paper, from a system default, or was inferred.
Skim it if you want to know what's paper-faithful vs. accommodation.
